# Liste des fonctionnalités — Site E-commerce Laravel/Inertia

Légende statut : 🟢 Prêt | 🟡 En cours | ⚪ À faire | 🔴 Bloqué
Priorité : **P0** = critique (MVP) | **P1** = important (V1.1) | **P2** = secondaire (V2+) | **P3** = long terme
Colonne **Dev** : **Backend** = Laravel/PHP uniquement | **Front** = React/Inertia uniquement | **Les deux**.
Numérotation `#.#` = section.tâche, continue sur tout le document (sert de référence stable dans les
commits/PR, ex. `feat(2.3): carrousel images produit`).

Stack cible : Laravel + Inertia + React + TypeScript, PostgreSQL, Tailwind CSS, Spatie
Permissions, admin Inertia/React custom (pas de Filament/Livewire), Stripe, Sendcloud, Resend, Redis/Horizon, Meilisearch (Scout),
Cloudinary, Zustand, Pest, Larastan, Sentry. Détail des choix et justification dans `ARCHITECTURE.md`,
schéma complet des tables dans `DATA_MODEL.md`, commandes d'installation dans `STACK.md`.

---

## Modèles de données & Migrations (vue d'ensemble)

Récapitulatif de toutes les tables de `DATA_MODEL.md`, pour vérifier d'un coup d'œil qu'aucune
n'est oubliée. Chaque table est réellement créée dans le cadre d'une tâche fonctionnelle
ci-dessous (pas de doublon de travail entre cette liste et les tâches numérotées) — la colonne
**Tâche** renvoie vers le `#.#` qui la migre.

| Table | Tâche | Statut |
| --- | --- | --- |
| `users` | 1.1/2.1 (starter) | 🟢 |
| `passkeys` | 2.1 (starter) | 🟢 |
| `roles` / `permissions` / `model_has_roles` / `model_has_permissions` / `role_has_permissions` | 2.2 | 🟢 |
| `brands` | 4.2 | 🟢 |
| `categories` | 4.2 | 🟢 |
| `product_lines` | 4.6 | 🟢 |
| `product_category` (pivot) | 4.2 | 🟢 |
| `products` | 4.1 | 🟢 |
| `product_options` | 4.2 | 🟢 |
| `product_option_values` | 4.2 | 🟢 |
| `product_variants` | 4.2 | 🟢 |
| `variant_option_values` (pivot) | 4.2 | 🟢 |
| `product_images` | 5.1 | 🟢 |
| `inventory_movements` | 4.4 | 🟢 |
| `carts` | 8.1 | 🟢 |
| `cart_items` | 8.1 | 🟢 |
| `cart_gift_items` | 10.2 | ⚪ |
| `gift_threshold_rules` | 10.2 | ⚪ |
| `gift_threshold_rule_rewards` | 10.2 | ⚪ |
| `addresses` | 9.1 | 🟢 |
| `orders` | 9.2 | 🟢 |
| `order_items` | 9.2 | 🟢 |
| `payments` | 9.2 | 🟢 |
| `refunds` | 16.3 | 🟢 |
| `shipments` | 11.2 | ⚪ |
| `coupons` | 10.1 | ⚪ |
| `articles` | 18.1 | ⚪ |
| `article_product` (pivot) | 18.2 | ⚪ |
| `newsletter_subscribers` | 13.2 | 🟢 |
| `wishlists` | 26.3 | 🟢 |

---

## PHASE 1 — Fondations

### 1. Setup & Configuration

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 1.1 | Setup Laravel + starter kit Inertia/React | Les deux | P0 | 🟢 | Starter en place (Fortify + passkeys + 2FA + shadcn/ui), Laravel 13.20.0. `composer run dev` lance bien `php artisan serve` + `queue:listen` + `npm run dev` (Vite) en parallèle via `concurrently`, `.env` déjà généré. |
| 1.2 | Setup PostgreSQL + migrations de base | Backend | P0 | 🟢 | `DB_CONNECTION=pgsql` en local (Neon, connexion directe via `DB_URL`, **jamais** l'endpoint `-pooler`). `.env.example` mis à jour. Migrations de base passées avec succès. Bug concrètement reproduit avec l'endpoint `-pooler` (mode transaction PgBouncer) : `migrate:rollback` a répondu "DONE" sans réellement dropper la colonne, provoquant un `Duplicate column` au remigrate — état corrigé manuellement. Confirme l'avertissement : connexion directe uniquement. |
| 1.3 | Setup Tailwind CSS | Front | P0 | 🟢 | Tailwind v4 (`@tailwindcss/vite`) + `tw-animate-css` + shadcn/ui/Radix confirmés dans `package.json`. `resources/css/app.css` correctement configuré (thème oklch, `@source` sur les vues). `npm run build` compile sans erreur, CSS généré dans `public/build/`. |
| 1.4 | Validation env variables | Backend | P1 | 🟢 | `php artisan config:cache` / `config:clear` vérifiés en local, aucune clé manquante. `.env.example` à jour pour les packages déjà installés (voir 1.2). Les clés Stripe/Cloudinary/Meilisearch/Sendcloud/Sentry/Resend seront ajoutées à `.env.example` au fur et à mesure de leur installation (Phases 2+), pas avant — voir `STACK.md` §5. |

### 2. Auth & permissions

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 2.1 | Auth (Fortify) — login/register/reset password | Les deux | P0 | 🟢 | `FortifyServiceProvider::configureViews()` OK, toutes les routes (login/register/forgot-password/reset-password/2FA/confirm-password) enregistrées et testées : pages Inertia répondent 200, création d'un `User` via factory + hash de mot de passe validés en base PostgreSQL. Envoi réel des emails de reset à revérifier une fois Resend branché (12.1) — actuellement `MAIL_MAILER=log`. |
| 2.2 | Rôles & permissions (Spatie) — admin/staff/support/customer | Backend | P0 | 🟢 | `spatie/laravel-permission` v8.3 installé, migration `create_permission_tables` exécutée, trait `HasRoles` ajouté à `User`. `RolePermissionSeeder` crée les 6 permissions (`products.manage`, `orders.manage`, `orders.refund`, `reviews.moderate`, `content.manage`, `settings.manage`) et les rôles `admin` (toutes), `staff` (produits/commandes), `support` (commandes/avis, pas catalogue ni finance) — `customer` reste implicite (pas de rôle Spatie). Assignation de rôle + vérification de permission testées bout en bout via `tests/Feature/RolePermissionTest.php` (5 tests Pest), suite complète (44 tests) toujours verte. |

### 3. Layout général

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 3.1 | Layout général storefront (Navbar, Footer) | Front | P1 | 🟢 | `resources/js/layouts/storefront/` (template) + `layouts/storefront-layout.tsx` (wrapper, même pattern que `app-layout.tsx`). `StorefrontHeader`/`StorefrontFooter` dans `components/storefront/`. Header alimenté par une liste `categoryNavItems: NavItem[]` vide pour l'instant — le mega-menu (24.3) n'aura qu'à peupler cette liste, pas à restructurer le header. Avatar/dropdown si connecté, sinon boutons connexion/inscription. `tsc`, `eslint` et `vite build` passent sans erreur ; pas encore de page consommatrice (catalogue = Phase 2). |

---

## PHASE 2 — Catalogue produits

### 4. Modèles de données catalogue

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 4.1 | Modèle Product (nom, description) | Backend | P0 | 🟢 | Table `products` migrée avec le schéma complet de `DATA_MODEL.md` (`brand_id` sans contrainte FK pour l'instant — ajoutée en 4.2 une fois `brands` créée). `status` en enum PHP natif `App\Enums\ProductStatus` (`draft`/`published`/`archived`), `SoftDeletes` — un produit retiré du catalogue ne doit jamais casser l'historique des `order_items` (snapshot du nom/prix). **Prix et stock vivent sur `product_variants` (4.2), pas sur `products`** — le titre de cette tâche est trompeur sur ce point, corrigé ici. Modèle + factory + 5 tests Pest (statut par défaut, publication, cast `skin_types`, soft delete, unicité du slug), Larastan clean. |
| 4.2 | Modèle Brand / Category / Variant (axes multiples) | Backend | P0 | 🟢 | `brands` (marque, `country_of_origin`) + FK `products.brand_id` ajoutée (différée depuis 4.1). `categories` en arbre (`parent_id` auto-référencé, relations `parent()`/`children()`), pivot `product_category` (many-to-many explicite, nom de table non conventionnel donc précisé dans `belongsToMany()` des deux côtés). Variantes modélisées en axes (`product_options` + `product_option_values` + pivot `variant_option_values`) pour supporter contenance **et** teinte sans migration supplémentaire — pas une simple colonne `label`. 5 modèles + 5 factories + 6 tests Pest (relation brand, arbre catégories, many-to-many produit/catégories, variante mono-axe, variante bi-axes, unicité SKU), Larastan clean. |
| 4.3 | Liste INCI ingrédients par produit | Les deux | P0 | 🟢 | Colonne `ingredients_inci` (text) sur `products` — déjà migrée en 4.1, donc pas de nouvelle colonne ici. Le vrai travail restant : `App\Observers\ProductObserver` (attaché via `#[ObservedBy]`) bloque toute sauvegarde d'un produit `status=published` sans `ingredients_inci` renseigné (`App\Exceptions\MissingInciListException`), y compris sur une mise à jour qui viderait le champ d'un produit déjà publié — obligation légale cosmétique appliquée au niveau modèle, donc valable pour tout point d'entrée (admin, seeders, API future), pas juste un formulaire. 5 tests Pest. Affichage front dans l'onglet dédié de la fiche produit reste à faire en 6.2. |
| 4.4 | Gestion stock (décrémentation auto) | Backend | P0 | 🟢 | Table `inventory_movements` (`product_variant_id`, `type` enum `App\Enums\InventoryMovementType`, `quantity` **signé** — positif réassort/retour, négatif vente —, `note`, `created_at` seul : ledger immuable, pas d'`updated_at`). `App\Services\StockService::recordMovement()` applique le delta à `product_variants.stock_quantity` dans une transaction avec `lockForUpdate()` (verrou pessimiste anti-survente en cas de ventes concurrentes) et lève `App\Exceptions\InsufficientStockException` si le stock deviendrait négatif — rollback automatique, aucun mouvement n'est enregistré. Le déclenchement réel (appel de `recordMovement()` avec `Sale`) se fera depuis le webhook Stripe (9.4), jamais au moment du `PaymentIntent` créé — ce mécanisme est le service générique, pas encore branché à un point d'entrée HTTP. 4 tests Pest (réassort, vente, survente refusée + stock inchangé, historique des mouvements). |
| 4.5 | Alertes stock bas | Backend | P2 | 🟢 | `App\Notifications\LowStockAlert` (canal `mail` uniquement pour l'instant — le vrai envoi Resend n'est branché qu'en 12.1, jusque-là `MAIL_MAILER=log`), déclenchée depuis `StockService::recordMovement()` **uniquement au franchissement** du seuil `config('inventory.low_stock_threshold')` (défaut 5, `LOW_STOCK_THRESHOLD` dans `.env`) — pas à chaque mouvement une fois déjà en dessous, pour ne pas spammer une alerte par vente. Envoyée à tous les `User::role('admin')` (Spatie, 2.2), pas à `staff`/`support`. 4 tests Pest (franchissement notifie, vente restant au-dessus ne notifie pas, vente restant en-dessous ne re-notifie pas, réassort qui repasse au-dessus ne notifie pas). Canal `database` + affichage dans le dashboard admin (16.1) laissés pour plus tard. |
| 4.6 | Gammes (collections de produits par marque) | Les deux | P2 | 🟢 | Tâche identifiée après coup, en s'inspirant de [uniikon.com](https://uniikon.com) (`DESIGN_GUIDE.md`) : une **gamme** (ex. "Advanced Snail" chez COSRX) regroupe des produits d'une même marque autour d'un thème/actif, orthogonale à `categories` (le type de produit). Table `product_lines` (`brand_id` FK `cascadeOnDelete`, `name`, `slug` auto-généré via `spatie/laravel-sluggable`, sur le modèle de `Category`/`Brand`) ; colonne `products.product_line_id` **nullable** (`nullOnDelete` — la majorité des produits n'appartiennent à aucune gamme identifiée). CRUD admin `Admin\ProductLineController` (`/admin/product-lines`, permission `products.manage`, même throttle que catégories/marques), sur le modèle exact de `Admin\CategoryController`. Sélecteur "Gamme" ajouté au formulaire produit (`admin/products/create.tsx`/`edit.tsx`), options affichées en "Marque — Gamme" (`ProductController::productLineOptions()`) pour rester lisible sans dépendre du filtre marque déjà sélectionné. Filtre `?product_line=slug` ajouté au catalogue storefront (`CatalogQuery`/`CatalogPresenter`/`catalog.tsx`), même pattern que le filtre marque existant (7.3). Volontairement laissé hors périmètre : page dédiée "Découvrir la gamme X" avec mega-menu (comme Uniikon) et kits/bundles par gamme — à ne construire que si l'usage réel le justifie (plusieurs gammes bien remplies), pour éviter une abstraction prématurée. 7 tests Pest (CRUD admin : liste, permission, création + slug, validation marque, mise à jour + slug, suppression détache les produits ; filtre catalogue par gamme). |

### 5. Médias produits

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 5.1 | Upload images produits (Cloudinary) | Les deux | P0 | 🟢 | `cloudinary-labs/cloudinary-laravel` incompatible avec Laravel 13 (dernière version ne supporte que `illuminate/support` `^11\|^12`) — utilisation directe du SDK officiel `cloudinary/cloudinary_php` à la place, encapsulé dans `App\Services\CloudinaryService` (`upload()`/`destroy()`/`url()`), instance `Cloudinary\Cloudinary` enregistrée en singleton dans `AppServiceProvider` à partir de `config('services.cloudinary.url')` (`CLOUDINARY_URL` dans `.env`). Ne stocke que le `public_id` en base (`product_images.path`) ; `ProductImage::url()` construit l'URL transformée (`w_/h_/c_fill/q_auto/f_auto`) à la volée, aucun fichier transformé dupliqué en stockage. Table `product_images` (`product_id`, `product_variant_id` nullable, `path`, `alt_text`, `position`) — pas de colonne `is_primary` dédiée : l'image principale est celle avec `position` la plus basse (0), utilisée telle quelle par le futur carrousel storefront (5.2). Upload fait depuis l'admin, sur la page produit (16.2b) : `App\Http\Controllers\Admin\ProductImageController` (`store` multi-fichiers en une requête, `makePrimary` qui décale les positions pour faire passer une image en tête, `destroy`) + `StoreProductImageRequest` (images ≤ 5 Mo chacune, `product_variant_id` optionnel restreint aux variantes du produit courant). Suppression Cloudinary + DB synchronisées (le mock `CloudinaryService` dans les tests évite tout appel réseau réel). `Product::primaryImage()` (`HasOne::ofMany('position', 'min')`) expose l'image de couverture sans dupliquer une requête par produit ; utilisée pour afficher une vignette dans `pages/admin/products/index.tsx` (absente jusqu'ici). 8 tests Pest, Larastan clean. |
| 5.2 | Carrousel images produit | Front | P0 | 🟢 | `resources/js/components/storefront/product-gallery.tsx` — carrousel principal + bande de miniatures + lightbox plein écran (`embla-carousel-react` via `components/ui/carousel.tsx`, shadcn/ui, ajouté ici). Consomme les images triées par `position` (relation `Product::images()`), filtre sur `product_variant_id` quand une variante est sélectionnée (retombe sur toutes les images si le filtre est vide). Lightbox : `components/ui/dialog.tsx` étendu avec `showCloseButton`/`overlayClassName` pour un rendu plein écran fond noir flouté, compteur d'images, miniatures propres, navigation clavier/flèches/swipe (héritée d'Embla). Construite avec une page produit storefront minimale pour la tester réellement (6.2 complet — onglets, avis — reste à faire) : layout `layouts/storefront-layout.tsx` enfin branché dans `app.tsx` (`name.startsWith('storefront/')`, absent jusqu'ici bien que les composants header/footer existaient déjà), route publique `GET produits/{product:slug}` (`routes/storefront.php`, nouveau, `require` depuis `web.php`), `App\Http\Controllers\Storefront\ProductController::show()` (404 si le produit n'est pas `published`). 4 tests Pest, Larastan clean. |

### 6. Pages publiques catalogue

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 6.1 | Page liste produits (catalogue public) | Les deux | P0 | 🟢 | `App\Http\Controllers\Storefront\CatalogController::index()` → `Inertia::render('storefront/catalog')` (minuscule, cohérent avec `storefront/product` de 5.2 — la casse `Catalog` du descriptif d'origine n'était pas suivie ailleurs dans le code). Route publique `GET produits` (`storefront.products.index`, avant `produits/{product:slug}` dans `routes/storefront.php`). Filtre `status = published` uniquement, pagination Laravel classique (`paginate(24)->withQueryString()`) — `withQueryString()` prépare le terrain pour les filtres (7.3) sans rien à changer ici. Chaque produit expose marque, prix (variante par défaut, même logique que `ProductController::show`) et vignette Cloudinary (image `position` la plus basse). Nouveau composant générique `components/pagination.tsx` (consomme directement le tableau `links` de Laravel, pas de primitive shadcn dédiée dans le starter) — réutilisable tel quel par un futur listing admin paginé. 3 tests Pest (liste avec marque/prix, exclusion des brouillons/archivés, pagination à 24/page), Larastan clean. |
| 6.2 | Page détail produit (onglets Bénéfices/Description/Ingrédients/Mode d'emploi/Avis) | Les deux | P0 | 🟢 | 5 onglets au final (pas 4) pour rester cohérent avec `CONTENT_STRUCTURE.md` §3 qui liste aussi "Mode d'emploi" — le champ `how_to_use` existait déjà en base (4.1) mais n'était affiché nulle part avant. `@radix-ui/react-tabs` ajouté + `components/ui/tabs.tsx` (nouvelle primitive shadcn/ui, absente du starter). `Storefront\ProductController::show()` expose désormais `ingredients_inci`/`how_to_use` en plus des champs déjà présents (5.2). Onglet "Avis" volontairement statique ("Aucun avis pour le moment") : le modèle `reviews` (Phase 6, 14-15) n'existe pas encore. 2 tests Pest ajoutés (contenu ingrédients/mode d'emploi exposé), suite existante de 5.2 inchangée, Larastan clean. |
| 6.3 | Sélecteur de quantité | Front | P0 | 🟢 | Nouveau composant `components/storefront/quantity-selector.tsx` (boutons +/- avec `lucide-react`, champ texte contrôlé), state React local sur `storefront/product`. Borné à `[1, stockQuantity]` côté client uniquement — `Storefront\ProductController::show()` expose désormais `stockQuantity` (variante par défaut). Rupture de stock (`stockQuantity` à 0 ou nul) : sélecteur masqué, message "Rupture de stock." affiché à la place. Revalidation serveur à l'ajout panier reste à faire en 8.1. 1 test Pest ajouté (stock exposé), Larastan clean. |
| 6.4 | Prix barré / promotions produit | Les deux | P1 | 🟢 | Colonne `compare_at_price_cents` déjà présente sur `product_variants` (4.1), fillable côté modèle et validée côté admin (`StoreProductVariantRequest`/`UpdateProductVariantRequest`) — seul l'affichage front et le formulaire admin manquaient. `Storefront\ProductController::show()` et `CatalogController::index()` exposent désormais `compareAtPriceCents` (variante par défaut). Affichage conditionnel (prix barré en `line-through`) sur `storefront/product` et `storefront/catalog` uniquement si renseigné et strictement supérieur au prix courant. Formulaire admin (`admin/products/edit.tsx`) : champ "Prix barré (centimes)" ajouté aux formulaires de création et d'édition de variante, et affiché en lecture seule dans la liste des variantes. 2 tests Pest ajoutés (exposition catalogue + fiche produit), Larastan clean. |
| 6.5 | Guide de choix produit (questionnaire) | Les deux | P2 | 🟢 | Nouvel enum `App\Enums\SkinType` (`seche`/`grasse`/`mixte`/`sensible`/`normale`/`terne`/`mature`/`deshydratee`/`acne`) — seule source de vérité des valeurs possibles pour `skin_types` (jsonb, 4.1), jusqu'ici jamais exposé ni validé nulle part. Page questionnaire `storefront/skin-guide` (route `GET guide-de-choix`, `Storefront\SkinGuideController`) : une question, un choix par type de peau + un lien "Tous les types de peaux" (pas de filtre), redirige en lien simple vers `/produits?skin_type=<valeur>` — pas de state serveur comme prévu au départ. `CatalogController::index()` lit ce paramètre de requête (`SkinType::tryFrom`, ignoré silencieusement si invalide) et filtre via `whereJsonContains('skin_types', ...)` ; expose `activeSkinType` pour afficher un badge de filtre actif + lien de retrait sur `storefront/catalog`. Formulaire admin produit (`create.tsx`/`edit.tsx`) complété avec des cases à cocher `skin_types[]` (le champ était fillable/casté en array depuis 4.1 mais aucun formulaire ne le renseignait) ; validation ajoutée dans `StoreProductRequest`/`UpdateProductRequest`. `CatalogDemoSeeder` mis à jour pour assigner 1 à 2 types de peau aléatoires par produit de démo. 5 tests Pest ajoutés (filtre catalogue, paramètre invalide ignoré, page questionnaire, création produit avec types de peau), Larastan clean. |
| 6.6 | Pages collection par marque (façon Uniikon) + filtre gammes | Les deux | — | 🟢 | **Hors périmètre initial**, demandé en cours de route (référence : pages collection Uniikon). "Gammes" = les `categories` existantes (4.2), pas un nouveau modèle — réutilisées telles quelles pour éviter la duplication catégorie/gamme. `App\Http\Controllers\Storefront\BrandController` : `index()` liste les marques ayant au moins un produit (`whereHas('products')` — un premier essai avec `withCount()->having()` cassait sous SQLite, `HAVING` sur requête non agrégée), `show()` réutilise directement `App\Support\CatalogQuery::paginate()`/`CatalogPresenter::present()` (même infra de filtre que 7.3) en forçant la marque, plus une liste de gammes scopée à cette marque pour les chips de filtre. Routes `GET marques` et `GET marques/{brand:slug}` (`routes/storefront.php`). Pages `storefront/brands-index.tsx` (annuaire marques) et `storefront/brand.tsx` (hero marque + chips gammes + filtres prix/tri + grille produits, réutilise `router.get(...)` avec `preserveState`). Lien "Marques" ajouté à la nav du header storefront (`storefront-header.tsx`, jusqu'ici emplacement réservé vide pour un futur mega-menu). 4 tests Pest (annuaire n'expose que les marques avec produits, page marque scope les produits publiés, filtre gamme scopé à la marque, `categoryOptions` n'expose que les gammes ayant des produits de cette marque), Larastan clean. Routes FR uniquement, pas de miroir `/en` (hors scope de la session i18n courante, 25.1). |

### 7. SEO & recherche catalogue

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 7.1 | Slugs SEO produits/catégories | Backend | P1 | 🟢 | `spatie/laravel-sluggable` (attribut `#[Sluggable(from: 'name', to: 'slug')]` sur `Product` et `Category`, génération automatique via les listeners d'events `eloquent.creating`/`eloquent.updating` du package). Colonne `slug` unique sur `products`/`categories`. Tests couvrant génération auto, collision (suffixe `-1`), et régénération à la mise à jour du nom. |
| 7.2 | Recherche produits (Scout + Meilisearch) | Les deux | P1 | 🟢 | `laravel/scout` + driver `meilisearch` (`config/scout.php`), trait `Searchable` sur `Product`. `shouldBeSearchable()` n'indexe que `status = published`, `toSearchableArray()` expose `name`/`short_description`. Sans `MEILISEARCH_HOST`/`MEILISEARCH_KEY` configurés, Scout retombe sur le driver `collection` (recherche en mémoire, insensible à la casse) — c'est le mode utilisé en local/CI (`phpunit.xml` fixe `SCOUT_DRIVER=collection`), sans dépendance à un serveur Meilisearch. `CatalogController::index()` combine la recherche (`Product::search($q)->keys()`) avec les filtres Eloquent existants (catégorie/marque/prix/tri) via un `whereIn('products.id', ...)`. Champ de recherche sur `/produits` avec recherche "live" (debounce 400ms, pas de bouton submit) et icône `lucide-react`. 2 tests Pest ajoutés (recherche par nom, recherche insensible à la casse sur la description courte), Larastan clean. |
| 7.3 | Filtres / tri catalogue | Les deux | P1 | 🟢 | Filtres catégorie/marque/`skin_types`/fourchette de prix + tri (prix croissant/décroissant, nom) sur `/produits`, synchronisés avec la query string via `router.get` (Inertia, `preserveState`). Le lien vers le détail produit et le bouton retour reconstruisent dynamiquement l'URL avec tous les filtres actifs. 4 tests Pest ajoutés (filtre catégorie, filtre marque, fourchette de prix, tri par prix), Larastan clean. |

---

## PHASE 3 — Panier & commande

### 8. Panier

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 8.1 | Panier serveur | Backend | P0 | 🟢 | `bumbummen99/shoppingcart` abandonné depuis 2022 (`illuminate/support` plafonné à `^9.0`, incompatible Laravel 13) — `darryldecode/cart` et `binafy/laravel-cart` évalués en remplacement mais écartés aussi (le premier plafonne encore à `^12.0` avec 198 issues ouvertes, le second n'a aucune notion d'invité et recalcule le prix en direct sans snapshot) : `CartService` natif à la place, sur les tables `carts`/`cart_items` telles que définies dans `DATA_MODEL.md`. Invité identifié par un cookie `cart_token` (`Str::random(64)`, chiffré comme tout cookie via `EncryptCookies`, ~400 jours), compte connecté par `user_id` (unique) — un seul panier par visiteur dans les deux cas. `CartService::addItem()`/`updateQuantity()` verrouillent la variante (`lockForUpdate()`) et revalident le stock disponible à chaque ajout/modif (`InsufficientStockException` → erreur de validation 422), complétant la revalidation serveur annoncée mais non faite en 6.3. Prix `unit_price_cents` snapshotté à l'ajout et rafraîchi à chaque cumul de quantité. `MergeGuestCartOnLogin` (écouteur de `Illuminate\Auth\Events\Login`, enregistré dans `AppServiceProvider::boot()`) fusionne le panier invité dans le panier du compte à la connexion — sans ça, le panier invité aurait été perdu au login. `Storefront\CartController` (index/store/update/destroy) + routes `GET/POST /panier`, `PATCH/DELETE /panier/{cartItem}` ; ownership vérifiée (403 si la ligne n'appartient pas au panier du visiteur courant). Page `storefront/cart` minimale (liste, `QuantitySelector` réutilisé de 6.3, suppression) — sans store Zustand, qui reste le périmètre de 8.3. 8 tests Pest (ajout, cumul de quantité, stock insuffisant à l'ajout et à la modification, mise à jour, suppression, isolation entre visiteurs, fusion à la connexion), Larastan clean. |
| 8.2 | Calculs monétaires (totaux, remises) | Backend | P0 | 🟢 | `brick/money` ajouté (`composer.json`). `CartItem::lineTotal(string $currency): Money` (`Money::ofMinor()->multipliedBy($quantity)`) remplace la multiplication `int` brute. `Cart::subtotal()`/`Cart::total()` retournent des `Money` (réduction avec `Money::zero($currency)`) ; `total()` est distinct de `subtotal()` dès maintenant — actuellement identique en l'absence de remise, mais le point d'insertion existe pour que 10.1 (coupons) et 10.2 (cadeaux à paliers) soustraient du sous-total sans changer les appelants. Méthodes `*Cents()` conservées (`getMinorAmount()->toInt()`) pour la sérialisation JSON/Inertia. `Storefront\CartController::index()` expose `totalCents` et `currency` en plus de `subtotalCents`. Front : helper partagé `resources/js/lib/money.ts` (`Intl.NumberFormat('fr-FR', { style: 'currency' })`) remplace le formatage `toFixed(2)` fait main dans `cart.tsx`. 3 tests Feature ajoutés (`CartMoneyTest`: ligne, somme multi-articles, panier vide), Larastan clean. |
| 8.3 | State panier frontend (Zustand) | Front | P0 | 🟢 | `resources/js/stores/cart-store.ts` : store partagé (items, subtotalCents, totalCents, currency, itemCount). État **optimiste** uniquement — `setQuantityOptimistic()`/`removeOptimistic()` mettent à jour l'UI immédiatement au clic, `sync()` resynchronise avec la réponse Inertia authoritative après coup, pour éviter toute divergence (en particulier sur les cadeaux à paliers, 10.2). `App\Support\CartPresenter` centralise la sérialisation panier (utilisée par `CartController::index()` ET par la prop Inertia partagée `cart`, ajoutée dans `HandleInertiaRequests::share()` via `CartService::findExisting()` — lecture seule, aucun panier/cookie créé pour un visiteur qui n'y touche pas). `resources/js/hooks/use-cart-actions.ts` centralise la mutation optimiste + l'appel serveur, réutilisé par `cart.tsx` et le nouveau `MiniCartSheet` (icône panier du header : badge du nombre d'articles + panneau `Sheet` avec aperçu des lignes, scrollable au-delà de 60vh, total et lien vers `/panier`). Larastan clean, 135 tests Pest toujours verts. |

### 9. Checkout & paiement

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 9.1 | Tunnel checkout (adresse, livraison) | Les deux | P0 | 🟢 | Table `addresses` (`user_id` nullable — conservé au niveau schéma, mais **le checkout invité a été retiré en 9.2**, voir sa note : `type` enum `App\Enums\AddressType` shipping/billing). `App\Http\Controllers\Storefront\CheckoutController::index()` redirige vers `/panier` si le panier est vide ; pré-remplit l'adresse de livraison par défaut (`is_default`) si l'utilisateur connecté en a une. `storeAddress()` (`StoreCheckoutAddressRequest`, validation miroir de zod) crée les lignes `addresses` (livraison + facturation, éventuellement identiques si `billing_same_as_shipping`) et stocke leurs ids en session pour l'étape paiement (9.2). **Sélection d'une adresse enregistrée** (ajouté après coup, une fois l'espace client Adresses disponible en 9.6) : `index()` expose désormais `savedShippingAddresses`/`savedBillingAddresses` (adresses du compte, triées adresse par défaut en tête) ; `storeAddress()` accepte `shipping_address_id`/`billing_address_id` (validés via `Rule::exists` scopé à `user_id`+`type`, donc l'ownership est garanti au niveau de la validation plutôt que par un `abort_if` a posteriori) en alternative à la saisie d'une nouvelle adresse — évite de ressaisir une adresse déjà enregistrée à chaque commande. "Même adresse que la livraison" continue à dupliquer la ligne choisie en une nouvelle ligne `billing` (comportement inchangé, y compris quand la livraison vient d'une adresse existante). Page `storefront/checkout` : formulaire `react-hook-form` + `zod` (`@hookform/resolvers` ajouté), champ adresse avec autocomplétion via l'API Adresse gouvernementale (`api-adresse.data.gouv.fr`, gratuite/sans clé, `components/storefront/address-autocomplete-input.tsx`) qui pré-remplit code postal/ville/pays à la sélection — garde anti-course (`requestId`) pour ignorer une réponse réseau arrivée après une plus récente ; sélecteur d'adresse en boutons radio natifs (livraison et facturation séparément) avec option "Nouvelle adresse" qui révèle le formulaire, présélection de l'adresse par défaut du compte si elle existe. Lien "Passer la commande" ajouté sur `storefront/cart`. 4 tests Pest ajoutés (adresses exposées par type, sélection d'une adresse existante, 403 sur l'adresse d'un autre utilisateur, sélection distincte livraison/facturation). **Mise à jour 2026-07-30** : `storeAddress()`/`sessionAddress()` appelaient `Address::query()->create()/find()` directement — fuite Clean Architecture repérée en auditant les controllers hors modules Orders/Cart/Stock (`ARCHITECTURE.md` §3bis). Corrigée en introduisant `Domain\Orders\Contracts\AddressRepositoryInterface` (`find`/`findOrFail`/`create`, `EloquentAddressRepository`), désormais injectée dans `CheckoutController` à la place des appels Eloquent directs — Checkout dispose ainsi d'un premier bout d'interface, au-delà de la simple extraction pragmatique initiale. |
| 9.2 | Paiement Stripe (Checkout / Payment Element) | Les deux | P0 | 🟢 | Tables `orders`/`order_items`/`payments` (`DATA_MODEL.md`), enums `OrderStatus`/`PaymentStatus`/`PaymentProvider`. `App\Actions\Orders\PlaceOrder` snapshote le panier (nom produit, libellé variante, prix) dans l'`Order` `pending` — jamais recalculé depuis le catalogue après coup, y compris si le produit change/est supprimé. `App\Services\StripeService` (client Stripe bindé en singleton dans `AppServiceProvider`) crée le `PaymentIntent` (`automatic_payment_methods`, laisse Stripe proposer CB/Wero/Apple Pay/Google Pay selon la config du dashboard). `CheckoutController::pay()` (`GET`+`POST /commande/paiement`, idempotent : réutilise la commande/le paiement `pending` existants au lieu d'en recréer à chaque rechargement de page ou nouvel essai — vérifie le statut réel du `PaymentIntent` via l'API Stripe avant de tenter de modifier son montant, Stripe refusant cette opération une fois `succeeded`) crée `Order`+`OrderItem`+`Payment`, `CheckoutController::confirmation()` sert le `return_url` de `stripe.confirmPayment()` : vide le panier et nettoie la session de checkout (adresses + order id) une fois le paiement confirmé côté Stripe — sans ce nettoyage, un nouveau panier retombait sur l'ancienne commande déjà payée. Le *statut* de la commande, lui, n'est jamais mis à jour depuis ces pages : seul le webhook Stripe (9.4) fait foi. Front : `StripePaymentForm` (`@stripe/react-stripe-js`) monte le Payment Element à partir du `clientSecret`, préremplit nom/email/téléphone/adresse déjà connus (`defaultValues`), affiche un état de chargement explicite et désactive le bouton tant que `stripe`/`elements` ne sont pas prêts (silencieux sinon). **Checkout invité retiré** : middleware `checkout.auth` (`App\Http\Middleware\RequireAccountForCheckout`) sur tout le groupe de routes `/commande*` — un visiteur non connecté est redirigé vers `/login` via `redirect()->guest()` (stocke l'URL demandée en session) et renvoyé automatiquement dessus après connexion/inscription grâce à `redirect()->intended()`, déjà utilisé par les réponses Fortify par défaut. Décision qui simplifie aussi l'email de confirmation (12.2) : toujours `$request->user()->email`, jamais de collecte d'email invité à ajouter. Corrections découvertes en testant en conditions réelles : (1) le schéma zod du formulaire de facturation masqué (`billing_same_as_shipping`) ne doit **pas** porter de contrainte `min(1)` sur des champs présents mais vides — seule une validation conditionnelle (`superRefine`) doit s'appliquer, sinon la soumission échoue silencieusement ; (2) **bouton "Ajouter au panier" manquant sur la fiche produit** depuis 6.3/8.1 (le sélecteur de quantité et l'endpoint existaient mais n'étaient jamais reliés) — ajouté ici avec toast de succès (`Inertia::flash('toast', ...)`, pattern déjà utilisé côté admin). 22 tests Pest (dont réutilisation de la commande pending, rechargement `GET` sans 405, `PaymentIntent` déjà `succeeded`, non-réutilisation après un nouveau panier, redirection invité + retour post-login), Larastan clean. **Mise à jour 2026-07-30** : `confirmation()` lisait `Payment::query()->where('order_id', ...)->latest('id')->first()` directement, en doublon de `PaymentRepositoryInterface` déjà utilisée ailleurs dans le module Orders — nouvelle méthode `findLatest(Order)` ajoutée à l'interface/`EloquentPaymentRepository`, utilisée à la place. |
| 9.3 | Apple Pay / Google Pay (Stripe Payment Element) | Les deux | P1 | ⚪ | Déjà couvert par `stripe-php`, uniquement de la config Payment Element côté front + domaine à vérifier dans le dashboard Stripe — pas de package supplémentaire. |
| 9.4 | Webhook Stripe (confirmation paiement) | Backend | P0 | 🟢 | `routes/webhooks.php` (`POST /stripe/webhook`), CSRF exclu via `bootstrap/app.php` (`validateCsrfTokens(except: ['stripe/webhook'])`) — reste dans le groupe `web` pour rester cohérent avec le reste du routing, mais Stripe pose sa propre signature donc le jeton CSRF de session n'a pas de sens ici. `App\Services\StripeService::constructWebhookEvent()` vérifie la signature `Stripe-Signature` (`Stripe\Webhook::constructEvent`) avant de faire confiance au payload — signature absente ou invalide → 400, jamais de traitement. `App\Http\Controllers\Webhooks\StripeWebhookController` : sur `payment_intent.succeeded`, retrouve le `Payment` via `provider_payment_id`, passe `Order.status` à `paid` et décrémente le stock de chaque `OrderItem` via `StockService::recordMovement()` (4.4) dans une transaction. Idempotent : Stripe peut livrer le même événement plusieurs fois, donc si le `Payment` est déjà `succeeded` l'événement est acquitté (200) sans re-décrémenter le stock. Un `PaymentIntent` sans `Payment` correspondant est loggé (`Log::warning`) et acquitté sans erreur plutôt que de faire échouer le webhook. Le *statut* de la commande n'est donc mis à jour que par ce webhook, jamais par le retour navigateur (`CheckoutController::confirmation()`, 9.2). Job Horizon pour l'email de confirmation (12.2) reporté à cette tâche : Horizon n'est pas installé (`QUEUE_CONNECTION=database`), Resend non plus (12.1) — rien à déclencher pour l'instant. 6 tests Pest (signature absente/invalide, événement non géré acquitté sans effet de bord, succès → statut + stock + mouvement, rejeu idempotent, `PaymentIntent` orphelin), Larastan clean. **Endpoint jamais enregistré côté Stripe avant le 2026-08-05** : `STRIPE_WEBHOOK_SECRET` était bien présent en prod mais ne correspondait à aucun `webhook_endpoint` réel (`GET /v1/webhook_endpoints` renvoyait une liste vide) — en pratique aucune commande n'aurait jamais pu passer à `paid` en prod. Créé via l'API Stripe (mode test) sur `https://boutique.nfdev.fr/stripe/webhook`, événement `payment_intent.succeeded` uniquement, secret régénéré et mis à jour en prod. À refaire côté clés `sk_live_`/`pk_live_` au passage en paiements réels (un webhook distinct par mode). **Bug corrigé (2026-08-05)** : le garde-fou anti-doublon de `ConfirmOrderPayment` ne vérifiait que `Payment.status === Succeeded` pour ignorer un événement rejoué — un paiement déjà `Refunded` ne correspondait pas à cette condition. Comme Stripe garantit une livraison "au moins une fois", un `payment_intent.succeeded` tardif ou dupliqué arrivant après un remboursement complet aurait redécrémenté le stock et remis la commande à `paid` sans passer par `RefundOrder` (même famille de bug que 16.3, trouvé en auditant les autres endroits où l'état `refunded` pourrait être quitté). Corrigé en ignorant aussi les paiements déjà `Refunded` dans ce garde-fou. |
| 9.5 | Historique commandes (espace client) | Les deux | P1 | 🟢 | `App\Http\Controllers\Storefront\AccountController` (`orders()` + `show()`), routes `mon-compte/commandes` et `mon-compte/commandes/{order}` protégées par `middleware('auth')` (pas `checkout.auth`, qui est spécifique au tunnel de commande). `orders()` paginate (10/page, `withQueryString`) les commandes de `$request->user()` (nouvelle relation `User::orders()`), triées par `placed_at` décroissant — inclut volontairement les commandes encore `pending` (paiement abandonné), le statut français (`OrderStatus::label()`, ajouté ici sur le modèle de `SkinType::label()`) rend cet état explicite plutôt que de les masquer. `show()` vérifie que la commande appartient bien à l'utilisateur (`abort_if(..., 403)`, même pattern que `CartController::authorizeCartItem()`) et affiche le détail complet : adresses de livraison/facturation, lignes avec prix unitaire, sous-total/remise/livraison/taxes/total. **Image produit par ligne de commande** : `order_items.product_image_path` (nouvelle colonne, migration séparée pour ne pas modifier une migration déjà mergée) snapshotée par `PlaceOrder` en même temps que `product_name`/`variant_label` — comme ces champs, elle ne doit plus jamais changer même si l'image du produit change/est supprimée par la suite ; resolue en URL Cloudinary à l'affichage via `CloudinaryService`, comme dans le catalogue. Pages `storefront/account-orders` (liste) et `storefront/account-order` (détail), sous `pages/storefront/` (et non `pages/account/` comme suggéré initialement) pour hériter automatiquement de `StorefrontLayout` via le switch de layout dans `app.tsx`. Lien "Mes commandes" ajouté dans `UserMenuContent`. Prépare le terrain pour le tracking colis (11.3), qui sera affiché sur cette même page de détail une fois `shipments` branché — rien à faire côté 9.5 pour ça, la structure est prête. 7 tests Pest (liste scoped à l'utilisateur + tri, libellé de statut français, état vide, redirection invité sur la liste et le détail, détail consultable par le propriétaire, 403 pour un autre utilisateur), Larastan clean. |
| 9.6 | Gestion des adresses (espace client) | Les deux | P1 | 🟢 | Tâche identifiée après coup : la note de 9.1 renvoyait explicitement une page "Mes adresses" à l'espace client (9.5), mais 9.5 n'a couvert que l'historique des commandes — jamais formalisée comme tâche numérotée jusqu'ici. `App\Http\Controllers\Storefront\AccountAddressController` (`index`/`store`/`update`/`destroy`), routes `mon-compte/adresses*` sous `middleware('auth')` (même groupe que 9.5). `AddressType::label()` ajouté (Livraison/Facturation), sur le modèle de `OrderStatus::label()`. Un seul type d'adresse par défaut par client : `is_default` à `true` désactive automatiquement l'ancien défaut du même `type` pour cet utilisateur (transaction), sinon `CheckoutController::index()` (9.1) ne saurait plus laquelle préremplir. Ownership vérifiée sur `update`/`destroy` (`abort_if(..., 403)`, même pattern que 9.5/`CartController`). Page `storefront/account-addresses` : liste en grille avec badge "Par défaut", formulaire création/édition dans une `Dialog` (shadcn/ui) réutilisant `AddressAutocompleteInput` (9.1). Lien "Mes adresses" ajouté dans `UserMenuContent`. Sélection d'une adresse enregistrée dans le tunnel de commande (9.1) volontairement laissée hors périmètre ici — le tunnel continuait à créer une nouvelle ligne `addresses` à chaque commande ; traité dans une itération suivante, voir la note ajoutée en 9.1. 8 tests Pest (accès invité, isolation + tri par défaut, création, désactivation de l'ancien défaut, modification, 403 sur modification/suppression d'une adresse d'un autre utilisateur, suppression), Larastan clean. |
| 9.7 | Reprise de paiement (commande en attente) | Les deux | P1 | 🟢 | Ajouté suite à une question client (2026-08-05) : une commande passe en base au statut `pending` dès le clic sur "Payer" (9.2), avant toute confirmation Stripe — carte refusée, 3D Secure abandonné ou onglet fermé la laissent durablement `pending`, rattachée au compte. L'espace client (9.5) n'offrait jusqu'ici aucun moyen de la débloquer, obligeant à reconstituer un panier complet sans lien avec la commande abandonnée. `CheckoutController::resume()` (`GET commande/{order}/reprendre`, miroir `/en`) : vérifie que la commande appartient au client connecté (403 sinon) et qu'elle est toujours `pending` (redirige vers sa fiche sinon), puis relit/recrée directement le `PaymentIntent` via `ProcessCheckoutPayment` (9.2, déjà idempotent) sans repasser par le panier ni l'étape adresse — réutilise l'adresse de livraison/facturation déjà snapshotée sur la commande. Rend la page `storefront/checkout` directement à l'étape `payment`. Lien "Continuer le paiement" affiché sur `account-order.tsx` quand `order.status === 'pending'`. 3 tests Pest (reprise réussie sans dupliquer la commande, 403 sur la commande d'un autre client, redirection si la commande n'est plus `pending`). |

### 10. Promotions & cadeaux

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 10.1 | Gestion codes promo / coupons | Les deux | P2 | ⚪ | Table `coupons` déjà prévue (`type` percentage/fixed, `usage_limit`, `times_used`). Application côté panier + revalidation au paiement (8.2). |
| 10.2 | Cadeaux à paliers (seuils panier) | Les deux | P2 | ⚪ | Module le plus coûteux du domaine panier — isolé dans `app/Actions/Cart/SyncThresholdGifts.php` (voir `ARCHITECTURE.md`/`DATA_MODEL.md`). Réévalue `gift_threshold_rules` à chaque recalcul panier, ajoute/retire les lignes `cart_gift_items` ; prix forcé à 0 côté commande, jamais modifiable par le client. |

---

## PHASE 4 — Livraison

### 11. Intégration Sendcloud

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 11.1 | Intégration Sendcloud (calcul tarifs) | Backend | P1 | ⚪ | Pas de SDK PHP officiel — appels REST via la façade `Http`, encapsulés dans `app/Services/SendcloudService.php`. Utilise `weight_grams` sur `product_variants` pour le calcul. |
| 11.2 | Génération étiquettes livraison | Backend | P1 | ⚪ | Déclenché depuis l'admin (16.3) une fois la commande `paid`/`processing`. Crée/complète l'enregistrement `shipments` (voir `DATA_MODEL.md`). |
| 11.3 | Suivi colis (tracking) | Les deux | P2 | ⚪ | `shipments.tracking_number`/`tracking_url` exposés côté espace client (9.5) et dans l'email d'expédition (12.3). |

---

## PHASE 5 — Emails & notifications

### 12. Emails transactionnels

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 12.1 | Setup Resend (mail driver) | Backend | P0 | 🟢 | `resend/resend-laravel` installé (`composer require`, puis `composer dump-autoload` nécessaire — le service provider n'était pas repéré tant que l'autoload n'était pas régénéré après le require). `MAIL_MAILER=resend`, clé lue via `RESEND_API_KEY` (nom de variable du package, différent du `RESEND_KEY` mentionné dans `STACK.md` — corrigé ici). Envoi réel testé en sandbox (`Mail::raw()` via tinker) : sans domaine vérifié sur resend.com/domains, Resend refuse tout destinataire autre que l'adresse propriétaire du compte — testé avec succès vers celle-ci. 2 tests Pest (`tests/Feature/ResendMailConfigTest.php` : transport `resend` enregistré, clé lue depuis `services.resend.key`). **Mise à jour 2026-07-30** : domaine `boutique.nfdev.fr` ajouté et vérifié sur Resend (SPF/DKIM/MX ajoutés côté DNS Squarespace, statut `verified` confirmé côté dashboard) — `MAIL_FROM_ADDRESS` bascule de l'adresse sandbox `onboarding@resend.dev` vers `noreply@boutique.nfdev.fr`. Envoi réel retesté via tinker vers une adresse externe (plus seulement le propriétaire du compte) : reçu, expéditeur correct. Débloque 12.2-12.5 pour de vrais clients. |
| 12.2 | Email confirmation commande | Backend | P0 | 🟢 | `App\Notifications\OrderConfirmation` (Notification native Laravel, `ShouldQueue` — passait par `QUEUE_CONNECTION=database` à l'écriture de cette tâche, bascule sur `QUEUE_CONNECTION=redis`/Horizon depuis 13.1) : liste les articles (nom/variante/total), sous-total, livraison, total. Déclenchée dans `StripeWebhookController::markAsPaid()` juste après le passage à `paid` (donc jamais renvoyée sur rejeu d'un événement déjà traité, l'idempotence du webhook s'applique aussi à l'email). Montants formatés à la main (`number_format`) plutôt qu'avec `Brick\Money::formatTo()`/`NumberFormatter` : l'extension `intl` n'est pas installée sur cet environnement. 2 tests Pest ajoutés à `StripeWebhookTest` (email envoyé au propriétaire de la commande, non renvoyé sur rejeu), Larastan clean. |
| 12.3 | Email expédition/suivi | Backend | P1 | ⚪ | Déclenché à la transition `shipments.status = shipped`, inclut `tracking_url` (11.3). |
| 12.4 | Email vérification de compte | Backend | P1 | 🟢 | Bug trouvé en vérifiant l'envoi réel : `App\Models\User` n'implémentait pas `Illuminate\Contracts\Auth\MustVerifyEmail` (import laissé commenté depuis le starter kit) — le middleware `verified` (`routes/web.php`, `routes/settings.php`) ne bloquait donc **aucun** compte non vérifié, `Features::emailVerification()` étant activé côté Fortify sans jamais être réellement appliqué. Corrigé en implémentant l'interface (le trait `Illuminate\Auth\MustVerifyEmail` est déjà fourni par `Illuminate\Foundation\Auth\User`, seule l'interface manquait). Envoi réel testé via tinker (`$user->notify(new VerifyEmail())`) vers l'adresse propriétaire du compte Resend — a aussi révélé que `MAIL_FROM_ADDRESS=hello@example.com` (placeholder de 12.1) fait échouer **tout** envoi Resend, pas seulement vers un destinataire non autorisé : le domaine `example.com` lui-même n'est jamais accepté comme expéditeur. Corrigé en basculant `MAIL_FROM_ADDRESS` sur `onboarding@resend.dev` (adresse sandbox fournie par Resend, utilisable sans domaine vérifié) — remplacé par `noreply@boutique.nfdev.fr` le 2026-07-30 une fois le domaine vérifié (voir 12.1). Tests Pest ajoutés (`VerificationNotificationTest`) : un utilisateur non vérifié est redirigé loin d'une route `verified`, un utilisateur vérifié y accède ; rendu du `MailMessage` via le vrai thème markdown (`Illuminate\Mail\Markdown`) pour détecter toute erreur de rendu, sans dépendre du réseau — `Mail::fake()` écarté car `MailChannel` envoie la vue brute (pas un `Mailable`), que `MailFake::sendMail()` ignore silencieusement. |
| 12.5 | Email reset password (vérification réelle) | Backend | P1 | 🟢 | Suite du point resté ouvert en 2.1, fermé en même temps que 12.4 (même cause `MAIL_FROM_ADDRESS`, même correctif `onboarding@resend.dev`). Envoi réel testé via tinker (`$user->notify(new ResetPassword(...))`). Test Pest ajouté (`PasswordResetTest`), même approche que 12.4 : rendu du `MailMessage` via le vrai thème markdown, vérifie que l'URL d'action contient bien le token. |
| 12.6 | Thème mail personnalisé (branding Kbeauty) | Backend | P2 | 🟢 | Thème publié (`php artisan vendor:publish --tag=laravel-mail`, `resources/views/vendor/mail/`) puis `html/themes/default.css` recoloré avec la charte réelle du site (`docs/DESIGN_GUIDE.md`) : fond `#faf9f7`, texte `#1a1a1a`, bordures `#e7e2dc`, bouton primaire `#1a1a1a`, succès `#7a9b76` (sage), erreur `#c97064` (rouge doux), panneau accent rose `#e8c4c4`/`#f6e4e4`. En-tête/salutations passés en serif (`Georgia`/`Times New Roman`, repli du `Fraunces` du site — pas de `@font-face` en email, non fiable selon les clients). Pas de logo image : aucun asset de marque n'existe encore (`app-logo-icon.tsx` est un placeholder du starter kit), le header reste donc le nom textuel `Korea Beauty`. Vérifié en rendant `Illuminate\Auth\Notifications\VerifyEmail::toMail()` réel via tinker : couleurs et police bien présentes dans le HTML généré. Impacte tous les emails existants et futurs (12.2-12.5, 13.2, 24.1) sans code à modifier côté notifications, le thème est global. |
| 12.7 | Alerte stock bas (admin) | Backend | P1 | 🟢 | Déjà implémenté mais jamais documenté ici : `App\Notifications\LowStockAlert` (`ShouldQueue`), envoyée via `App\Application\Stock\UseCases\RecordStockMovement::notifyIfCrossingLowStockThreshold()` à tous les admins au moment où le stock d'une variante franchit `config('inventory.low_stock_threshold')` à la baisse (déclenche une seule fois par franchissement, pas à chaque décrément sous le seuil). **Mise à jour 2026-07-30** : la liste des admins était récupérée via `User::role('admin')->get()` directement dans le use case — fuite Clean Architecture (voir §3bis d'`ARCHITECTURE.md`), corrigée en passant par la nouvelle `Domain\Shared\Contracts\UserRepositoryInterface::admins()` (`EloquentUserRepository`). Même correction appliquée à `ConfirmOrderPayment` (notification `NewPaidOrderAlert`, fuite identique déjà repérée lors d'un audit précédent) et à `MergeGuestCartIntoUserCart` (requête Eloquent directe sur les lignes du panier invité, remplacée par `CartRepositoryInterface::itemsWithVariant()`). |
| 12.8 | Notification admin : nouvelle commande payée | Backend | P1 | 🟢 | `App\Notifications\NewPaidOrderAlert` (`ShouldQueue`), envoyée à `User::role('admin')` (même pattern que 12.7) depuis `StripeWebhookController::markAsPaid()`, juste après `OrderConfirmation` au client. Contient le numéro de commande, le montant et un lien vers `admin.orders.show`. Test Pest ajouté dans `StripeWebhookTest` : les admins sont notifiés, le staff ne l'est pas. |
| 12.10 | Email confirmation de remboursement (client) | Backend | P1 | 🟢 | `App\Notifications\RefundConfirmation` (`ShouldQueue`), envoyée au client depuis `RefundOrder::__invoke()` uniquement quand le remboursement passe à `RefundStatus::Succeeded` (un remboursement Stripe resté `Pending` ne notifie pas). Salutation horaire ("Bonjour"/"Bonsoir" + nom du client) via `App\Support\Salutation::selonHeure()`, également appliquée à `OrderConfirmation` (12.2) par cohérence — à réutiliser sur les prochains emails clients (12.4, 12.5, 24.1...). Tests Pest dans `AdminOrderTest` (remboursement partiel, total, et pending sans notification). |
| 12.11 | Alerte jobs Horizon en échec (admin) | Backend | P2 | 🟢 | Horizon lui-même n'a de notification native que pour `LongWaitDetected` (file bloquée) — pas pour un job en échec (`Laravel\Horizon\Events\JobFailed` existe mais n'a aucun listener de notification fourni par le package). Deux mécanismes distincts activés dans `HorizonServiceProvider::boot()` : (1) `Horizon::routeMailNotificationsTo()` activé conditionnellement via `config('services.horizon.alert_email')` (`HORIZON_ALERT_EMAIL` en `.env`, vide = désactivé), pour les alertes natives de Horizon (file bloquée) ; (2) nouveau listener `App\Listeners\NotifyAdminsOfFailedHorizonJob` sur `JobFailed`, qui envoie `App\Notifications\HorizonJobFailedAlert` (nom du job, connexion, queue, message d'exception, lien vers `/horizon/failed`) à tous les admins via `UserRepositoryInterface::admins()` (même pattern que 12.7/12.8/12.10) — c'est ce deuxième mécanisme qui couvre le vrai besoin de la tâche ("un job qui échoue... ne remonte nulle part"). Testé (`HorizonJobFailedAlertTest`) en appelant le listener directement plutôt que via `Event::dispatch()` : les propres listeners internes de Horizon abonnés au même événement (`MarkJobAsFailed`, `StoreTagsForFailedJob`) écrivent dans Redis à partir d'un payload de job complet, absent dans un événement simulé en test. |

### 13. Queues & newsletter

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 13.1 | Queues Horizon + Redis | Backend | P1 | 🟢 | `laravel/horizon` installé (`composer.json` : `"config": {"platform-check": false}` nécessaire — Horizon exige `ext-pcntl`/`ext-posix`, absentes sous Windows, et sans cette option `vendor/composer/platform_check.php` fait planter l'appli au boot ; installé avec `--ignore-platform-req=ext-pcntl --ignore-platform-req=ext-posix`). `predis/predis` ajouté comme client Redis (pur PHP, pas d'extension native `ext-redis` requise — utile sous Windows). `QUEUE_CONNECTION=redis`, `REDIS_CLIENT=predis`. **Environnement de dev sous Windows : Horizon ne peut pas tourner nativement** (`pcntl`/`posix` n'existent pas sur les builds PHP Windows) — le projet reste développé/testé sous Windows comme avant, mais `php artisan horizon` et le serveur `redis-server` tournent dans WSL (Ubuntu 24.04 + PHP 8.5 via le PPA `ondrej/php`, le dépôt officiel Ubuntu ne fournissant que PHP 8.3, insuffisant : le code utilise la syntaxe *property hooks* de PHP 8.4+), en pointant sur les mêmes fichiers via `/mnt/c` (`vendor/` déjà généré côté Windows, pas besoin de Composer/Node côté Linux). Windows peut lui aussi joindre ce Redis via `127.0.0.1` grâce au relais réseau de WSL2, donc `php artisan queue:work` fonctionne aussi bien des deux côtés. Testé bout en bout : job `App\Notifications\OrderConfirmation` (12.2) poussé sur la queue Redis, traité par Horizon (visible `RUNNING` dans les logs, désenfilé avec succès). Correction associée : `config/horizon.php` généré par `horizon:install` — `Str::slug(env('APP_NAME', 'laravel'), '_')` passait un `bool\|string` à un paramètre `string`, cast ajouté (même pattern déjà utilisé dans `config/database.php` pour `REDIS_PREFIX`), Larastan clean. **Mise en prod (2026-08-05) et incident OOM :** aucun process Horizon n'existait sur Laravel Cloud avant cette date (`QUEUE_CONNECTION` absent des variables d'env prod ⇒ retombait sur `database`) — créé via `background-process:create` (`php artisan horizon`), mais avec `maxProcesses: 10` (valeur par défaut du bloc `environments.production` ci-dessous) l'instance `flex-512mb` est partie en boucle de redémarrage OOM (`App cluster starting...` répété toutes les 1 à 5 min dans `environment:logs`, RAM insuffisante pour nginx + php-fpm + jusqu'à 10 workers Horizon simultanés). Process supprimé en urgence, `maxProcesses` réduit à 2 dans `config/horizon.php` (volume de jobs de ce projet trop faible pour justifier davantage), puis process recréé. Laravel Cloud exige aussi d'activer explicitement `usesScheduler` sur l'instance (`instance:update --uses-scheduler=true`) — sans ça, **aucune** tâche de `routes/console.php` ne s'exécute jamais, contrairement à l'hypothèse initiale d'un scheduler managé actif par défaut (cf. 22.4/22.5). |
| 13.2 | Newsletter (opt-in) | Les deux | P2 | 🟢 | Table `newsletter_subscribers` (`email` unique, `consent_at`, `unsubscribed_at`). Formulaire dans `StorefrontFooter` (email + case à cocher consentement RGPD non pré-cochée, requise via la règle `accepted`), endpoint `POST /newsletter` (miroir `/en/newsletter`) throttlé (`10,1,storefront-newsletter`). Voir 13.3 pour le flux de confirmation (le simple envoi du formulaire ne pose plus `consent_at` depuis 13.3). |
| 13.3 | Confirmation d'inscription newsletter (double opt-in) | Backend | P2 | 🟢 | `consent_at` n'est plus renseigné à la soumission du formulaire (13.2) mais uniquement en cliquant le lien reçu par email — `App\Notifications\NewsletterSubscriptionConfirmation` (`ShouldQueue`) envoie un lien signé (`URL::temporarySignedRoute`, expire sous 7 jours) vers `GET /newsletter/confirmer/{subscriber}` (middleware `signed`, pas de miroir `/en` : les notifications ne sont pas encore localisées dans le projet, voir `OrderConfirmation`). `NewsletterController::store` reste idempotent : un abonné déjà confirmé et actif ne reçoit pas de second email ; une réinscription après désabonnement repasse par une nouvelle confirmation. `NewsletterSubscriber` utilise désormais `Notifiable` (routage mail via `email`, comme `User`). |
| 13.4 | Anti-spam formulaires publics (honeypot + Turnstile) | Les deux | P2 | 🟢 | `spatie/laravel-honeypot` sur `POST /newsletter` (miroir `/en`), via le middleware `Spatie\Honeypot\ProtectAgainstSpam` (`honeypot_fields_required_for_all_forms` activé pour cette route uniquement : une soumission sans les champs honeypot est rejetée). Formulaire React (pas de vue Blade) : les champs (nom de champ piège randomisé + horodatage chiffré `valid_from`) sont exposés via un prop Inertia partagé `honeypot` (`HandleInertiaRequests`) et envoyés en JSON par `StorefrontFooter` — pas d'`<input>` caché dans le DOM, donc rien à extraire pour un bot qui ne charge pas la page. **Responder corrigé** : le `BlankPageResponder` par défaut du package renvoie une réponse vide sans header `X-Inertia`, ce qui casse silencieusement une requête XHR Inertia (aucune erreur visible, formulaire qui semble ne rien faire) — remplacé par `App\Support\Honeypot\InertiaSpamResponder` (`config/honeypot.php`), qui fait un `redirect()->back()->withErrors(['spam' => ...])`, cohérent avec le flux Inertia normal. **Cloudflare Turnstile ajouté en complément** (le honeypot seul ne bloque qu'un bot naïf qui remplit tous les champs) : composant `resources/js/components/turnstile-widget.tsx` (`forwardRef`/`useImperativeHandle`, expose `reset()` — nécessaire car un token Turnstile est à usage unique, sans `reset()` explicite après chaque soumission le widget reste bloqué en `Succès !` sans jamais réémettre de token, obligeant à recharger la page), rendu sur les 3 formulaires publics sensibles : contact (26.5), newsletter (ci-dessus) et inscription (2.1). Règle de validation serveur `App\Rules\ValidTurnstileToken` (vérifie le token auprès de l'API Cloudflare `siteverify`) appliquée dans `SendContactMessageRequest`/`SubscribeNewsletterRequest`/`CreateNewUser`. Sans `VITE_TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY` configurées (env local/CI), le widget ne rend rien et la règle laisse passer — formulaires utilisables sans Turnstile configuré. Login volontairement exclu du périmètre : déjà protégé par le rate-limiting natif de Fortify, un risque différent (brute-force credentials, pas spam de contenu) pour lequel un CAPTCHA apporte peu par rapport à la gêne UX. 4 tests Pest (`ValidTurnstileTokenTest`, un par formulaire). |

---

## PHASE 6 — Avis clients

> Décision 2026-07-23 : pas de modèle `Review` maison — intégration de deux SaaS en parallèle,
> **Trustpilot** (avis + relance email post-achat) et **Klaviyo Reviews** (nouvel outil, ajouté en
> plus de Resend qui reste sur les emails transactionnels, pas un remplacement). Aucune table
> `reviews`/`review_photos` côté app : la collecte, la modération anti-fake-review et le stockage
> des avis sont délégués aux deux plateformes. Le backend n'a qu'à leur fournir les données
> nécessaires (déclenchement post-achat, identifiants produit) et à afficher leurs widgets.

### 14. Trustpilot

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 14.1 | Compte Trustpilot Business + config domaine | — | P1 | 🟡 | Prérequis côté utilisateur, hors code : créer/configurer le Business Unit Trustpilot, obtenir le Business Unit ID et les clés API (invitations + widgets). Business Unit créé sur `boutique.nfdev.fr` (meta tag de vérification posé, voir commit `391fda8`) et SMA configuré (14.3). Rendez-vous prévu le 2026-08-05 avec Trustpilot pour obtenir `TRUSTPILOT_BUSINESS_UNIT_ID`/`TRUSTPILOT_API_KEY`/`TRUSTPILOT_API_SECRET` (accès Developer Hub, nécessaires pour 14.2). |
| 14.2 | Widget TrustBox (fiche produit + pages clés) | Frontend | P1 | ⚪ | Intégration du script TrustBox Trustpilot sur les pages produit et éventuellement l'accueil, avec le Business Unit ID en config `.env`. Bloqué en attente des clés API (14.1, rendez-vous 2026-08-05). |
| 14.3 | Invitation à laisser un avis post-achat | Backend | P1 | 🟢 | Implémenté via le **Service Mail Automatique (SMA)** de Trustpilot plutôt que par appel direct à l'API Reviews : `TRUSTPILOT_INVITATION_BCC` (adresse propre à l'entreprise, fournie par Trustpilot dans Paramètres > Réglages des invitations > SMA) mise en BCC de `App\Notifications\OrderConfirmation` (`config('services.trustpilot.invitation_bcc')`, conditionnel si la variable est vide). Trustpilot extrait l'email client depuis la copie BCC et gère lui-même la mise en file d'attente (délai par défaut de 7 jours avant envoi) et l'envoi de l'invitation — aucun appel API sortant nécessaire côté backend. Testé en conditions réelles : invitation visible en file d'attente côté dashboard Trustpilot suite à un envoi de test. |

### 15. Klaviyo Reviews

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 15.1 | Compte Klaviyo + clés API | — | P1 | ⚪ | Prérequis côté utilisateur, hors code : créer le compte Klaviyo (nouvel outil, distinct de Resend), activer Klaviyo Reviews, obtenir clé API privée + clé publique (site ID). |
| 15.2 | Tracking événements Klaviyo (`Placed Order`/`Ordered Product`) | Backend | P1 | 🟢 | `App\Services\KlaviyoService` (appel `POST /api/events/`) + `App\Jobs\SendPlacedOrderEventToKlaviyo` (`ShouldQueue`), dispatché depuis `ConfirmOrderPayment` juste après les notifications de confirmation (`OrderConfirmation`/`NewPaidOrderAlert`). Tests Pest (`KlaviyoServiceTest` avec `Http::fake`, + assertion de dispatch dans `StripeWebhookTest`). Code fonctionnel mais **inactif tant que 15.1 n'est pas fait** : `KLAVIYO_PRIVATE_KEY`/`KLAVIYO_PUBLIC_KEY` encore vides en `.env`, donc aucun événement réellement envoyé à Klaviyo pour l'instant. |
| 15.3 | Flow de demande d'avis (Klaviyo) | — | P1 | ⚪ | Configuré côté Klaviyo (no-code, flow builder), pas de code applicatif — déclenché par l'événement 15.2. |
| 15.4 | Widget Klaviyo Reviews (fiche produit) | Frontend | P1 | ⚪ | Intégration du widget d'affichage des avis Klaviyo Reviews sur les pages produit, avec la clé publique en config `.env`. |

---

## PHASE 7 — Gestion interne (admin)

> L'admin est construit avec **Inertia + React (custom)**, le même stack que le storefront — pas
> de Filament/Livewire ni d'autre framework admin externe (voir `ARCHITECTURE.md` §3). C'est un
> troisième dossier de pages (`resources/js/pages/admin/`) protégé par un middleware de rôle
> Spatie sur `routes/admin.php`, pas un sous-site indépendant. D'où le type **Les deux** : chaque
> écran nécessite un contrôleur/Form Request (back) et une page React (front), il n'y a pas de
> génération automatique façon Filament.

### 16. Back-office admin (Inertia/React)

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 16.1 | Dashboard admin | Les deux | P0 | 🟢 | `routes/admin.php` (`require` depuis `web.php`, même pattern que `settings.php`) monté sur `/admin` derrière `auth` + `role:admin\|staff\|support` — alias `role`/`permission`/`role_or_permission` de Spatie ajoutés explicitement dans `bootstrap/app.php` (pas auto-enregistrés en Laravel 11+ sans Kernel HTTP, sinon `Target class [role] does not exist.`). `App\Http\Controllers\Admin\DashboardController` renvoie `admin/dashboard` avec des stats catalogue (`productsCount`, `publishedProductsCount`, `lowStockVariantsCount` via le seuil de 4.5). Layout `layouts/admin/admin-layout.tsx` + wrapper `layouts/admin-layout.tsx` (même convention gabarit/wrapper que `storefront-layout.tsx`), branché dans `app.tsx` (`name.startsWith('admin/')`). `components/admin/admin-sidebar.tsx` réutilise `AppShell`/`AppContent`/`NavMain`/`NavUser` existants. `components/admin/data-table.tsx` générique sur `components/ui/table.tsx` (primitive shadcn/ui ajoutée ici, absente avant). Rôles de l'utilisateur exposés au front via `auth.roles` (nouveau, dans `HandleInertiaRequests`) pour un scoping futur de la sidebar (16.2/16.3). 6 tests Pest (guest redirigé, rôle manquant → 403, les 3 rôles admin/staff/support accèdent, stats correctes), Larastan clean. **Refonte design 2026-07-30** : `DashboardController` enrichi (CA 30j, commandes/commandes en attente, CA par jour sur 14 jours, 5 dernières commandes, 5 variantes en stock bas) ; page `admin/dashboard.tsx` reconstruite avec `components/admin/stat-card.tsx` (cartes avec icônes `lucide-react`) et `components/admin/revenue-chart.tsx` (`recharts`, voir 17.3). Nouveaux composants partagés `components/admin/page-header.tsx` (titre/description/actions, appliqué à toutes les pages admin) réutilisés sur produits/marques/catégories/commandes ; `data-table.tsx` avec en-tête collant et conteneur arrondi. Formulaires produits/marques/catégories (create/edit) regroupés en sections `Card` avec barre d'actions collante en bas. `components/admin/admin-header.tsx` (nouveau) remplace `AppSidebarHeader` pour l'admin : ajoute le menu utilisateur (avatar + dropdown) en haut à droite au lieu du bas de la sidebar, et un bouton hamburger animé (`components/admin/animated-menu-button.tsx`) au lieu du simple bascule statique. Charte graphique `docs/DESIGN_GUIDE.md` implémentée dans `resources/css/app.css` (tokens `@theme`, remplace le thème shadcn gris par défaut) : fond blanc cassé chaud, texte quasi-noir, accent rose poudré décliné en échelle de 7 teintes (`--rose-50` à `--rose-600`, évite une seule teinte plate), plus tokens `success` (vert doux), `brand-sage`/`brand-gold` (badges, ex. "Vedette" sur `admin/products/index.tsx`) — impacte toute l'app (storefront compris), pas seulement l'admin. Nettoyage du starter-kit Laravel : liens morts "Repository"/"Documentation" (vers le repo GitHub de Laravel) retirés de `AppHeader`/`AppSidebar`, remplacés par une navigation réelle (dashboard, commandes, adresses). Bascule vue liste/grille ajoutée sur produits (`components/admin/product-grid.tsx`, avec vignette), marques, catégories et commandes (`components/admin/entity-grid.tsx`, générique) via `ToggleGroup`. Recherche + filtres ajoutés sur les 4 listings admin (`components/admin/list-filters.tsx`, debounce 350ms, synchronisé à l'URL via `router.get` + `preserveState`) : recherche texte partout (`name->fr` pour les produits, traduisible), filtre par statut sur produits/commandes, tri (récents/nom/prix croissant-décroissant) sur produits avec prix minimum des variantes affiché (`withMin('variants', 'price_cents')`). |
| 16.2 | CRUD produits/catégories | Les deux | P0 | 🟢 | **Catégories (16.2a) :** `App\Http\Controllers\Admin\CategoryController` (resource, sans `show`) + `StoreCategoryRequest`/`UpdateCategoryRequest`, protégé par `permission:products.manage` (nouveau sous-groupe dans `routes/admin.php` — exclut `support`, qui n'a pas cette permission). Slug auto-généré via `spatie/laravel-sluggable` (attribut `#[Sluggable(from: 'name', to: 'slug')]`, cohérent avec le style `#[Fillable]`/`#[ObservedBy]` du projet). `Category::descendantIds()` empêche un cycle dans l'arbre. Pages `pages/admin/categories/{index,create,edit}.tsx`. 8 tests Pest. **Produits (16.2b) :** `App\Http\Controllers\Admin\ProductController` (resource, sans `show`) + `StoreProductRequest`/`UpdateProductRequest` — mêmes champs que `Product` (4.1) plus `category_ids[]` synchronisé sur le pivot `product_category`. `#[Sluggable]` ajouté à `Product` (absent jusqu'ici — bug réel trouvé en testant en local : `slug` NOT NULL en base, `ProductFactory` le renseignait explicitement donc les tests ne l'avaient jamais remarqué). `ingredients_inci` rendu `required_if:status,published` côté validation (fait écho à `ProductObserver`/4.3, mais en amont pour un message d'erreur exploitable au lieu de l'exception 500 `MissingInciListException`). Axes de variantes gérés via `App\Http\Controllers\Admin\ProductOptionController` (`store`/`destroy` imbriqués sous `/admin/products/{product}/options`, values saisies en zone de texte une par ligne côté front) et les variantes via `App\Http\Controllers\Admin\ProductVariantController` (`store`/`update`/`destroy` sous `/admin/products/{product}/variants`, SKU unique, association aux valeurs d'axes via `optionValues()->sync()`). Après création/modification d'un produit, redirection vers sa page d'édition (pas la liste) pour enchaîner directement sur ses axes/variantes — comportement volontaire, confirmé avec l'utilisateur. Pages `pages/admin/products/{index,create,edit}.tsx`, `edit.tsx` réunit formulaire produit + sections axes/variantes sur la même page. 13 tests Pest (accès `support` refusé, listing, création avec slug auto, publication sans INCI rejetée/acceptée, mise à jour catégories, suppression, ajout/suppression d'axe, ajout/unicité SKU/modification/suppression de variante). `tests/Feature/ProductTest.php::the slug must be unique` réécrit en `the slug is auto-generated and unique-suffixed on collision` (l'ancien test attendait une `QueryException` sur collision de slug, obsolète depuis l'ajout de `#[Sluggable]`). Larastan clean. L'upload Cloudinary (5.1) suit dans une 3e branche. **Mise à jour 2026-07-30** : les listes admin produits et catégories chargeaient tout le dataset via `->get()` (repéré en auditant la pagination du projet, `admin/orders` était déjà paginé mais pas celles-ci) — passées à `->paginate(20)->withQueryString()`, pages front mises à jour pour consommer `products.data`/`categories.data` et afficher le composant `<Pagination>` (déjà utilisé sur 6.1/16.3). |
| 16.3 | Gestion commandes (statuts, remboursements) | Les deux | P0 | 🟢 | Table `refunds` (`order_id`/`payment_id` fk, `amount_cents`, `reason` nullable, `status`, `created_at` seul — même pattern ledger que `inventory_movements`) + enum `RefundStatus`. `App\Http\Controllers\Admin\OrderController` : `index` (liste paginée avec somme des remboursements réussis via `withSum`), `show` (détail complet : articles, adresses, paiements, remboursements), `updateStatus` (`permission:orders.manage`, `Rule::enum(OrderStatus::class)->except(OrderStatus::Refunded)` — le statut `refunded` ne peut jamais être choisi manuellement, uniquement dérivé d'un remboursement total), `refund` (`permission:orders.refund`, séparée de `orders.manage` : `staff`/`support` peuvent gérer une commande mais pas la rembourser, seul `admin` a les deux). `App\Actions\Orders\RefundOrder` : appelle `StripeService::refundPayment()` (nouveau, `stripe->refunds->create()`) sur le dernier paiement `succeeded`, crée le `Refund`, et ne fait passer la commande + le paiement à `refunded` que lorsque la somme des remboursements réussis atteint le total payé — un remboursement partiel laisse le statut de la commande inchangé. Formulaire de remboursement (page `admin/orders/show`) : montant + motif, protégé par une modale de confirmation shadcn/ui (pas de `window.confirm()` — remboursement Stripe réel et irréversible) via le `submit()` exposé par les slot props du composant `<Form>` d'Inertia. Le formulaire de remboursement n'est affiché que si la commande a au moins un paiement `succeeded` (`hasSucceededPayment`) — sans ce garde-fou, une commande encore `pending` affichait un formulaire de remboursement qui échouait silencieusement côté serveur. Lien "Commandes" ajouté à la sidebar admin (visible par les 3 rôles, `orders.manage` est partagé). 10 tests Pest (guest redirigé, `support` peut lister/voir mais pas rembourser, somme des remboursements exposée, mise à jour de statut, `refunded` rejeté en saisie manuelle, remboursement partiel n'affecte pas le statut, remboursement total marque commande+paiement `refunded`, montant dépassant le remboursable rejeté, commande sans paiement réussi non exposée comme remboursable / rejetée à l'envoi), Larastan clean. Génération d'étiquette Sendcloud (11.2) et export facture PDF (17.1) restent hors périmètre ici. **Bug corrigé (2026-08-05)** : `refunded` était bien bloqué en *entrée* manuelle (`Rule::enum(...)->except(OrderStatus::Refunded)`) mais rien n'empêchait de la faire *sortir* de cet état une fois atteinte — une commande déjà remboursée pouvait être repassée manuellement à un autre statut (ex. "Expédiée") via ce même formulaire, cassant l'invariant "remboursée = état terminal". Corrigé symétriquement : `UpdateOrderStatusRequest::withValidator()` rejette tout changement de statut si la commande est déjà `refunded`, et la page `admin/orders/show` affiche un message en lecture seule à la place du formulaire dans ce cas. Trouvé en signalement direct, pas en audit. |
| 16.4 | CRUD marques (admin) | Les deux | — | 🟢 | **Hors périmètre initial, ajouté en cours de route** pour supporter les pages collection par marque (6.6). `App\Http\Controllers\Admin\BrandController` (resource, sans `show`) + `StoreBrandRequest`/`UpdateBrandRequest`, même pattern exact que `CategoryController` (16.2a) : `permission:products.manage`, slug auto-généré (`#[Sluggable(from: 'name', to: 'slug')]` ajouté à `Brand`, absent jusqu'ici). Champs : nom, pays d'origine, description, logo (URL texte simple — pas de pipeline d'upload Cloudinary dédié pour l'instant). Pages `pages/admin/brands/{index,create,edit}.tsx`, lien "Marques" ajouté à la sidebar admin. 6 tests Pest (permissions, listing avec compteur produits, création + slug, suffixe de collision, mise à jour + slug, suppression met `brand_id` à `null` via `nullOnDelete()`), Larastan clean. **Mise à jour 2026-07-30** : listing paginé (`->paginate(20)->withQueryString()`) au lieu de `->get()`, voir 16.2. |
| 16.5 | Audit trail admin (activity log) | Les deux | P3 | 🟢 | `spatie/laravel-activitylog` (`LogsActivity`) sur `ProductVariant` (stock, log `stock`), `Order` (statut, log `order`), `Refund` (création, log `refund`) et `Product` (statut de publication, log `product`) — un seul champ pertinent par modèle (`logOnly` + `logOnlyDirty`), causer résolu automatiquement via `Auth::user()` (aucune config requise). La création (`created`) est exclue de l'audit (`$doNotRecordEvents`) pour ne tracer que les changements sur une ressource existante, pas sa création initiale — sauf pour `Refund`, immuable, où c'est justement la création qui doit être tracée. Page de consultation `GET /admin/activity-log` (`ActivityLogController`, réservée au rôle admin strict comme la page Santé), filtrable par type, avec lien vers la ressource concernée. |

### 17. Documents & exports

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 17.1 | Facturation PDF (dompdf) | Backend | P1 | 🟢 | `barryvdh/laravel-dompdf`. Génération automatique dès qu'une commande est confirmée payée (`ConfirmOrderPayment`, webhook Stripe `payment_intent.succeeded`) via le use case `App\Application\Orders\UseCases\GenerateOrderInvoice`, branché derrière `InvoicePdfRendererInterface`/`InvoiceRepositoryInterface` (Clean Architecture pragmatique, même pattern que le module Orders). `App\Infrastructure\Orders\DompdfInvoiceRenderer` génère le PDF depuis le snapshot `orders`/`order_items`, `EloquentInvoiceRepository` stocke l'entrée `Invoice` (numéro, chemin) sur le disque `invoices`. Téléchargeable côté client (`AccountController::downloadInvoice`, propriétaire de la commande uniquement) et côté admin (`Admin\OrderController`). Pas de pièce jointe à l'email `OrderConfirmation` (12.2) — lien de téléchargement uniquement. |
| 17.2 | Export CSV (commandes, clients) | Backend | P2 | ⚪ | Laravel natif (`Storage`/`Response::streamDownload`), pas de package. |
| 17.3 | KPIs / stats ventes (Recharts) | Les deux | P2 | 🟢 | `recharts` installé (`npm install recharts`). `components/admin/revenue-chart.tsx` : graphique en aires du chiffre d'affaires (commandes payées/en préparation/expédiées/livrées) sur les 14 derniers jours, affiché sur `admin/dashboard.tsx` (voir 16.1). |

---

## PHASE 8 — Contenu & SEO

### 18. Blog

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 18.1 | Blog (modèle Article) | Backend | P2 | ⚪ | Table `articles` déjà prévue (`status` draft/published, `cover_image_path`). |
| 18.2 | Liaison article ↔ produit (cross-sell éditorial) | Backend | P2 | ⚪ | Pivot `article_product`. |

### 19. SEO technique

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 19.1 | Sitemap XML | Backend | P1 | 🟢 | Un `SitemapController` maison existait déjà (fondations SEO), remplacé par `spatie/laravel-sitemap` (v8, toujours activement maintenu — vérifié avant migration) pour profiter du split automatique au-delà de 50k URLs et d'une API plus robuste. Génère : pages statiques (accueil, catalogue, marques, guide de choix, pages légales), produits publiés, marques ayant au moins un produit publié (bug corrigé au passage : l'ancienne requête `whereHas('products')` n'excluait pas les brouillons) — chaque URL FR est mirée avec son équivalent `/en/...` via `<xhtml:link rel="alternate" hreflang>` (`Url::addAlternate`) pour éviter tout signal de contenu dupliqué aux moteurs de recherche, cohérent avec la traduction complète du storefront (25.1/PR #119). Pas de « categories » séparées : ce sont des filtres `?category=`/`?product_line=` sur `/produits`, dont le canonical (`seo-head.tsx`) retombe déjà sur l'URL sans query string — les lister à part créerait du contenu dupliqué. Pas d'« articles » : le blog (18.1/18.2) n'existe pas encore. `robots.txt` corrigé au passage (`Disallow: /en/mon-compte` manquant, oubli lors de l'ajout du miroir EN). Tests Pest (`SitemapTest`) : pages statiques + produit publié + marque présents des deux côtés FR/EN avec hreflang, produit brouillon et marque sans produit publié absents. |
| 19.2 | Meta tags dynamiques (SEOTools) | Les deux | P1 | 🟢 | `artesaos/seotools` écarté au profit du composant React `SeoHead` déjà créé lors des fondations SEO (commit 5459112) : ce projet est en CSR pur (pas de SSR Inertia configuré), donc un package qui injecte des balises côté Blade n'aurait rien de plus à offrir que ce que `SeoHead` fait déjà via les props Inertia (`meta_title`/`meta_description` sur `products`, `seo` sur les autres pages) — l'ajouter aurait juste crée deux systèmes concurrents pour le même besoin (vérifié maintenu, v1.4.1 mars 2026, Laravel 10-13, mais pas adapté à l'architecture ici). `SeoHead` était déjà branché sur les 6 pages storefront principales (accueil, catalogue, marque, liste des marques, produit, guide de choix) ; les 5 pages légales (mentions légales, CGV, confidentialité, livraison, retours) n'avaient qu'un `<Head title>` nu sans description/canonical/OG — comblé en ajoutant une prop `description` à `LegalPage`, traduite via `t()` (FR en clé, `lang/en.json` pour l'anglais, même convention que le reste du storefront). |

---

## PHASE 9 — Pages légales & conformité

### 20. Pages légales

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 20.1 | Mentions légales | Front | P0 | 🟢 | Page statique Inertia (`storefront/legal/mentions-legales`), pas de modèle back. Infos d'identité (raison sociale, SIRET, hébergeur...) en placeholders `[À COMPLÉTER]` — aucune donnée légale réelle disponible dans le repo, à remplir avant lancement public. |
| 20.2 | CGV | Front | P0 | 🟢 | Idem — contenu statique (`storefront/legal/cgv`), 9 articles (objet, prix, commande, paiement, livraison, rétractation/retours, garanties, données personnelles, litiges), liens internes vers 20.4/20.5/20.3/20.1. |
| 20.3 | Politique de confidentialité (RGPD) | Front | P0 | 🟢 | `storefront/legal/confidentialite` : responsable de traitement, données collectées, finalités/bases légales, sous-traitants (Stripe/Sendcloud/Resend), durées de conservation, droits RGPD, section cookies liée à 20.6. |
| 20.4 | Politique de livraison | Front | P0 | 🟢 | `storefront/legal/livraison` : zones, délais, frais, suivi, gestion colis endommagé/manquant. |
| 20.5 | Politique de retours/remboursements | Front | P0 | 🟢 | `storefront/legal/retours` : droit de rétractation (14 jours), exclusion produits cosmétiques descellés, procédure de retour, délai/modalités de remboursement, cas produit non conforme. |
| 20.6 | Bandeau consentement cookies | Front | P0 | 🟢 | Composant React custom (`CookieConsentBanner`, pas de package tiers) monté dans `StorefrontLayoutTemplate`, persistance du choix (`accepted`/`rejected`) en `localStorage` via `lib/cookie-consent.ts`, qui expose `hasMarketingConsent()` comme garde-fou pour bloquer le chargement des pixels marketing tant que le consentement n'est pas donné (voir `ARCHITECTURE.md` §8 et tâche 27.1). Les 5 pages légales (20.1-20.5) sont désormais liées dans `StorefrontFooter`. 5 tests Pest (une par route), CI clean. |

---

## PHASE 10 — Qualité & monitoring

### 21. Tests & qualité

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 21.1 | Tests (Pest) | Backend | P1 | ⚪ | Déjà dans le starter. Couvrir en priorité les Actions non triviales : `SyncThresholdGifts` (10.2), `PlaceOrder`, `RefundOrder`. |
| 21.2 | Analyse statique (Larastan) | Backend | P2 | ⚪ | Déjà dans le starter (`composer ci:check`). |

### 22. Monitoring

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 22.1 | Monitoring erreurs (Sentry) | Les deux | P1 | 🟢 | `sentry/sentry-laravel` (backend) + `@sentry/react` (frontend), même DSN des deux côtés (`SENTRY_LARAVEL_DSN` / `VITE_SENTRY_DSN`), événements distingués par le tag `environment` (auto-dérivé d'`APP_ENV` côté back, d'`import.meta.env.MODE` côté front) — pas de DSN distinct par environnement contrairement à la formulation initiale de la tâche, c'est l'usage standard Sentry (un projet = un DSN, plusieurs environnements dedans). Backend : `SentryIntegration::handles($exceptions)` câblé dans `bootstrap/app.php` (`withExceptions`), config publiée telle quelle (`config/sentry.php`, tout piloté par env). Frontend : `Sentry.init()` dans `resources/js/app.tsx`, conditionné par la présence du DSN (`VITE_SENTRY_DSN` vide en local/CI ⇒ SDK inerte, aucun envoi), `browserTracingIntegration` activée. `SENTRY_TRACES_SAMPLE_RATE=1.0` par défaut (à réduire en prod si le volume de trafic le justifie). Le wizard interactif `sentry:publish` bloquait en arrière-plan (attendait une entrée interactive) — config publiée manuellement via `vendor:publish --provider="Sentry\Laravel\ServiceProvider"` à la place. Reste à faire au déploiement : créer le projet Sentry (compte à créer), renseigner le vrai DSN en variable d'environnement Laravel Cloud + build frontend, avant toute mise en ligne publique (Jalon 2 dans `ROADMAP.md`). |
| 22.2 | Debug local (Telescope/Debugbar) | Backend | P1 | 🟢 | `laravel/telescope` + `barryvdh/laravel-debugbar` installés en `--dev` (`composer require --dev`) : absents d'un `composer install --no-dev` en prod, première protection "jamais en prod". `php artisan telescope:install` enregistre par défaut `App\Providers\TelescopeServiceProvider` sans condition dans `bootstrap/providers.php` — retiré de là et déplacé dans `AppServiceProvider::register()` derrière `if ($this->app->isLocal())` (2e protection, utile si des deps dev se retrouvent installées par erreur en staging/prod). Protection native du package en 3e rideau : `Telescope::check()`/`LaravelDebugbar::canBeEnabled()` n'autorisent respectivement l'accès/l'activation qu'en environnement `local` (Debugbar exclut aussi explicitement `testing`). Migration `create_telescope_entries_table` exécutée. `storage/debugbar/` (stockage fichier par défaut de Debugbar) ajouté au `.gitignore`, absent jusqu'ici. **Vérifié que les deux packages ne s'activent jamais pendant les tests** (`APP_ENV=testing` dans `phpunit.xml` ≠ `local`, donc `isLocal()` faux et `canBeEnabled()` de Debugbar exclut `testing` explicitement) — `TELESCOPE_ENABLED=false`/`DEBUGBAR_ENABLED=false` ajoutés à `phpunit.xml` par sécurité supplémentaire malgré tout. **Ralentissement local observé et non résolu** en installant ces packages (suite Pest passée de ~30s à ~175s sur cette machine) : investigué en profondeur (code source des deux packages confirmé inactif en environnement `testing`, processus PHP/WSL orphelins de la session tués, WSL arrêté) sans trouver la cause exacte — probablement propre à cet environnement Windows (OneDrive/antivirus scannant les ~150 nouveaux fichiers `vendor/`), pas un défaut de configuration. Le vrai juge de paix reste le CI GitHub Actions (runner Linux propre) : à surveiller sur les prochaines exécutions. |
| 22.3 | Rate limiting sur les endpoints publics/sensibles | Backend | P0 | 🟢 | Identifié après coup en auditant le rate limiting existant : Fortify ne throttle nativement que login/2FA/passkeys (`FortifyServiceProvider::configureRateLimiting()`) — `register`/`password.email` (demande de reset) n'avaient aucun throttle, ouvrant à la création de comptes en masse et à l'email bombing. Idem côté storefront : `POST /panier` (spam `cart_items`), `commande/paiement` (appelle l'API Stripe à chaque requête, coût direct + risque de flag "suspicious activity"), `mon-compte/adresses` n'avaient aucune protection. Throttle basique Laravel ajouté partout (`routes/storefront.php`, `routes/webhooks.php`) — **avec un préfixe explicite sur chaque groupe** (`throttle:X,1,<prefix>`) : le throttle basique ne clé que sur IP/utilisateur (pas sur la route), donc sans préfixe distinct tous les groupes `throttle:X,1` partageraient le même compteur pour un même visiteur. Panier 30/min (`storefront-cart`), checkout 20/min (`storefront-checkout`), adresses compte 30/min (`storefront-account-address`), webhook Stripe 120/min (`stripe-webhook`, protection anti-flood générique — la signature Stripe fait déjà foi). Pour `register`/`password.email` (routes auto-enregistrées par Fortify, pas définies dans nos fichiers de routes) : middleware attaché post-hoc sur les routes déjà enregistrées, via `FortifyServiceProvider::throttleUnprotectedRoutes()`, différé dans `$this->app->booted(...)` — bug réel rencontré en le développant : sans `RouteServiceProvider` legacy (ce projet utilise `bootstrap/app.php`/`ApplicationBuilder::withRouting()`), rien n'appelle jamais `RouteCollection::refreshNameLookups()` automatiquement, donc `Route::getRoutes()->getByName(...)` reste sur l'index construit *avant* que Fortify ne chaîne `->name(...)` sur ses routes et retourne toujours `null` (silencieusement, à cause du `?->`) — seule commande comme `route:list` le rafraîchit explicitement. Corrigé en appelant `refreshNameLookups()` avant le `getByName()`. Cote admin (`routes/admin.php`), egalement audite et completement nu au depart : `orders.refund` (declenche un remboursement Stripe reel et irreversible — throttle 10/min, `admin-refund`) et `products.images.store` (upload Cloudinary, cout direct — throttle 20/min, `admin-product-images`) proteges de la meme facon. 7 tests Pest (`RateLimitingTest`), dont un qui verifie explicitement que les buckets panier/checkout sont bien separes (sature le panier puis verifie que le checkout repond toujours). Larastan clean. **Mise à jour 2026-07-30** : le reste de l'admin (CRUD produits/categories/marques + options/variants, changement de statut commande), initialement laissé sans throttle car jugé à faible risque, a finalement été protégé suite à un 2e audit — `throttle:60,1,admin-catalog-write` sur le CRUD catalogue, `throttle:30,1,admin-order-status` sur `orders.update-status` (plus strict car déclenche des effets de bord : emails, mouvements de stock). |
| 22.4 | Sauvegardes automatiques (DB + fichiers) | Backend | P1 | 🟢 | Ajouté proactivement suite à l'incident de la session précédente (`migrate:fresh` lancé par erreur sur la base Neon distante, croyant tester en local) — pas de filet de sécurité avant ça. `spatie/laravel-backup` : dump PostgreSQL (`pg_dump`, compressé gzip) + fichiers du repo (exclut `vendor/`, `node_modules/`, `.git`, `storage/framework`, `storage/logs`), zippés et envoyés sur un disque dédié `backups` (`config/filesystems.php`) — **`local` en dev, `s3` (bucket Cloudflare R2 distinct de celui des factures, `BACKUP_BUCKET`) en prod**, jamais le même bucket qu'une resource qu'une sauvegarde est censée pouvoir restaurer. **Point de sécurité trouvé en testant réellement `backup:run`** : le comportement par défaut du package inclut `base_path()` en entier, donc `.env` (clés Stripe/Resend/R2/DB en clair) se retrouvait dans l'archive non chiffrée — exclu explicitement (`.env`, `.env.backup`) ; en cas de restauration les secrets viennent de la config d'environnement de Laravel Cloud, pas d'une sauvegarde. Vérification post-zip activée (`verify_backup`). Planifié via `routes/console.php` (`Schedule::command`, pas de `Kernel.php` en Laravel 13) : `backup:run` 2h, `backup:clean` 2h30 (stratégie par défaut : 7j complet, 16j quotidien, 8 sem. hebdo, 4 mois mensuel, 2 ans annuel), `backup:monitor` 3h — notifie par email (`BACKUP_ALERT_EMAIL`, même compte admin que 12.11) si une sauvegarde échoue ou dépasse 1 jour sans nouvelle. Suppose que Laravel Cloud exécute le scheduler managé (à vérifier au déploiement). 2 tests Pest (`BackupConfigTest`) : `.env`/`.env.backup` bien exclus, disque `backups` bien distinct. **Identifiants R2 dédiés** (2026-08-01) : le disque `backups` utilisait d'abord les mêmes clés `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` que le disque `invoices` — corrigé avec `BACKUP_ACCESS_KEY_ID`/`BACKUP_SECRET_ACCESS_KEY` dédiées, un token R2 scopé sur `kbeauty-invoices` n'ayant pas accès à `kbeauty-backups`. **Bug chemins absolus** : `relative_path` à `null` par défaut stockait le chemin Windows complet (`C:\Users\...`) comme nom d'entrée dans le zip — l'extracteur natif Windows refusait d'ouvrir l'archive ("invalide"), corrigé en passant `relative_path` à `base_path()` (vérifié avec une extraction réelle via `Expand-Archive`). **🟢 Problème critique découvert puis résolu en vérifiant le contenu réel du dump (2026-08-01)** : le dump PostgreSQL généré était vide (0 octet une fois décompressé) sur toutes les sauvegardes prises jusque-là — `pg_dump` 17.4 (installé sur la machine de dev Windows) refuse de dumper le serveur Neon qui tourne en PostgreSQL 18.4 (`pg_dump: erreur : annulation à cause de la différence de version`), pg_dump ne pouvant dumper qu'un serveur de version égale ou inférieure à la sienne — autrement dit **le contenu réel de la base n'avait jamais été sauvegardé**, seuls les fichiers du repo l'étaient. Résolu en installant `postgresql-client-18` côté WSL Ubuntu (dépôt officiel `apt.postgresql.org` — le dépôt Ubuntu 24.04 par défaut ne fournit que la 16 ; `pg_dump` installé par la 18 n'écrasait pas le binaire par défaut du wrapper `postgresql-common`, résolu avec un symlink `/usr/local/bin/pg_dump` prioritaire dans le `PATH`). Re-testé de bout en bout **depuis WSL** (`php artisan backup:run`, même PHP 8.5 déjà utilisé pour Horizon/13.1) : dump vérifié en clair (108 produits avec leurs vraies données), sauvegarde complète envoyée sur R2, `backup:list` saine. **Piège à retenir : ce correctif ne vaut que côté WSL.** Les commandes artisan lancées depuis un terminal Windows classique utilisent toujours `pg_dump` 17 (installé sous `C:\Program Files\PostgreSQL\17`) et produiraient de nouveau un dump vide — le scheduler local (`routes/console.php`) doit donc tourner depuis WSL, pas Windows, tant que `pg_dump` n'est pas aussi mis à jour côté Windows. **Vérifié au déploiement (2026-08-05)** : `backup:run` exécuté manuellement en prod via `cloud command:run` — succès, dump non vide, 9,74 Mo envoyés sur R2, `backup:list` confirme une sauvegarde saine. En revanche `usesScheduler` était à `false` sur l'instance Laravel Cloud depuis la mise en prod (23/07) : aucune tâche planifiée (`backup:run`/`clean`/`monitor`, `telescope:prune`, `health:check`) ne s'était jamais exécutée automatiquement — l'hypothèse "Laravel Cloud exécute le scheduler managé" ci-dessus était fausse, il faut l'activer explicitement (`instance:update --uses-scheduler=true`, voir aussi 13.1). Activé et redéployé, confirmé stable depuis. |
| 22.5 | Dashboard de santé applicative | Backend | P2 | 🟢 | `spatie/laravel-health` (installé avec `--ignore-platform-req=ext-pcntl --ignore-platform-req=ext-posix`, même contrainte que Horizon sous Windows, 13.1). `App\Providers\HealthServiceProvider` enregistre 6 checks : `DatabaseCheck`, `RedisCheck`, `QueueCheck`, `HorizonCheck`, `UsedDiskSpaceCheck`, `BackupsCheck` (`onDisk('backups')`, échoue si aucune sauvegarde `< 24h`, réutilise le disque dédié de 22.4). Vue HTML du package exposée sur `/admin/health`, réservée au rôle `admin` strict (pas `staff`/`support` — info d'infra, hors périmètre du reste du back-office). `QueueCheck` a besoin d'un heartbeat régulier pour savoir si la queue tourne vraiment : `health:queue-check-heartbeat` pousse un job de test sur Redis, `health:check` exécute tous les checks et notifie par email (`HEALTH_TO_ADDRESS`) en cas d'échec — les deux planifiés `everyMinute()` dans `routes/console.php`, à côté des tâches `backup:*` déjà en place. Notifications throttlées à 1/heure par le package (évite le spam si un check reste en échec). **Bug de la migration publiée par le package** : `EloquentHealthResultStore::getHistoryItemInstance()` n'a pas de type de retour déclaré côté vendor, Larastan le voit comme `object` et refuse `->getTable()` — corrigé en instanciant directement `HealthCheckResultHistoryItem` (même modèle, déjà utilisé juste au-dessus pour la connexion), sans `@phpstan-ignore` ni cast. Testé en local : `DatabaseCheck`/`RedisCheck`/`UsedDiskSpaceCheck` passent, `HorizonCheck`/`BackupsCheck`/`QueueCheck` échouent tant qu'Horizon n'a pas tourné et qu'aucun heartbeat/sauvegarde récente n'existe sur cette machine — comportement attendu (Horizon ne tourne pas nativement sous Windows, 13.1). 4 tests Pest (`HealthCheckTest`) : les 6 checks sont bien enregistrés, `/admin/health` répond 200 en admin/403 en staff/redirige en invité. **Bug `BackupsCheck` trouvé en prod (2026-08-05)** : `->onDisk('backups')` sans `->locatedAt(...)` liste uniquement la racine du disque (`Storage::files()` n'est pas récursif), or `spatie/laravel-backup` range chaque sauvegarde dans un sous-dossier nommé d'après `config('backup.backup.name')` (ex. `kbeauty-shop/2026-...zip`) — le check échouait donc systématiquement en "No backups found" même avec des sauvegardes réelles et saines sur le disque. Corrigé en ajoutant `->locatedAt(config('backup.backup.name'))`, test dédié ajouté (sauvegarde fake dans le sous-dossier, vérifie que le check la trouve). Après ce correctif + l'activation du scheduler et d'Horizon en prod (13.1/22.4), les 6 checks passent tous au vert sur `/admin/health`. |

---

## PHASE 11 — Fidélisation & croissance (grand site complet)

> P3 = ambitions long terme, à ne considérer qu'une fois le cœur de l'expérience (P0/P1) stable
> et le trafic réel observé — évite de construire des systèmes de fidélité/abonnement pour un
> site qui n'a pas encore de clients récurrents.

### 23. Fidélisation

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 23.1 | Cartes cadeaux (gift cards) | Les deux | P2 | ⚪ | Extension du modèle `coupons`/`orders`, à concevoir (pas encore dans `DATA_MODEL.md`). Inclut l'email de livraison du code au destinataire (personne différente de l'acheteur si "offrir"). |
| 23.2 | Programme de fidélité (points/paliers) | Les deux | P2 | ⚪ | Module custom, pas de package identifié — à modéliser au moment venu. |
| 23.3 | Parrainage / referral | Les deux | P2 | ⚪ | Code de parrainage + réduction filleul/parrain, probablement branché sur le système `coupons`. Inclut l'email au parrain quand une récompense est débloquée (filleul ayant commandé). |

### 24. Relance & merchandising

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 24.1 | Relance panier abandonné (email automatisé) | Backend | P1 | 🟢 | Seuil retenu : 2h d'inactivité, une seule relance par "épisode" d'abandon (pas de séquence). `App\Application\Cart\UseCases\SendAbandonedCartReminders` : sélectionne les paniers rattachés à un compte (`user_id` non nul — un panier invité n'a pas d'email), non vides, dont le dernier `cart_item` modifié n'a pas bougé depuis le seuil ; nouvelle colonne `carts.abandoned_cart_reminder_sent_at` évite de renvoyer l'email à chaque exécution tant que le panier n'a pas été retouché après le dernier envoi (comparaison avec le dernier `cart_items.updated_at`, pas un simple flag booléen — permet une seconde relance légitime si le client rouvre son panier puis l'abandonne à nouveau). `App\Notifications\AbandonedCartReminder` (`ShouldQueue`) liste les articles et pointe vers `/panier`. Commande `cart:send-abandoned-reminders` (`App\Console\Commands\SendAbandonedCartRemindersCommand`), planifiée toutes les 30 min (`routes/console.php`, `onOneServer()` comme les autres tâches planifiées de 13.1/22.4) — assez fréquent par rapport au seuil de 2h pour rester précis sans complexité de séquence. **Cas "paiement déjà commencé" (2026-08-05)** : un panier reste non vide tant que le paiement n'a pas réussi (le nettoyage n'intervient qu'à la confirmation Stripe, 9.2), donc un panier abandonné juste après le clic "Payer" a en réalité déjà une `Order` `pending` associée (9.7). Renvoyer ce client vers `/panier` recréerait une 2e commande en doublon plutôt que de reprendre celle déjà entamée. `SendAbandonedCartReminders` recherche désormais la dernière `Order` `pending` du client ; si elle existe, l'email pointe vers la reprise de paiement (9.7, `commande/{order}/reprendre`) au lieu du panier. 10 tests Pest (7 initiaux + lien vers le panier sans commande en cours, lien vers la reprise de paiement quand une commande `pending` existe, bout en bout via `SendAbandonedCartReminders`). |
| 24.2 | Bundles / routines packagées | Les deux | P1 | ⚪ | Nouveau regroupement de `product_variants` à prix réduit — à modéliser (pas encore dans `DATA_MODEL.md`). |
| 24.3 | Mega-menu catégories (navigation riche) | Les deux | P1 | 🟢 | `App\Support\MegaMenuPresenter::categories()` : catégories racines ayant au moins un produit publié (directement ou via un enfant), avec leurs enfants scopés eux aussi aux produits publiés — sinon un lien du menu redirigerait vers un catalogue vide. Partagé globalement via `HandleInertiaRequests` (prop `megaMenuCategories`, même pattern que `cart`/`honeypot`). `components/storefront/mega-menu.tsx` remplace le lien plat "Produits" du header par un `NavigationMenuTrigger` ouvrant une grille de catégories (`NavigationMenu` Radix déjà présent dans le starter), repli sur un simple lien si aucune catégorie n'est éligible ; miroir dans le menu mobile (`Sheet`). Données actuelles 100% plates (20 catégories racines, aucun enfant réel) mais le code supporte un niveau d'imbrication dès qu'il existera. 5 tests Pest (`MegaMenuTest`), Larastan clean. |
| 24.4 | Recommandations personnalisées | Les deux | P2 | ⚪ | "Souvent achetés ensemble" / "tu pourrais aimer" — logique à définir (co-achat via `order_items`, ou service tiers). |

### 25. Internationalisation

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 25.1 | Multi-langue (FR/EN) | Les deux | P2 | 🟢 | Infra posée + 3 sessions de traduction, storefront public désormais entièrement bilingue. **Infra :** middleware `locale:{fr,en}` (préfixe `/en`, groupe `Route::prefix('en')->name('en.')->middleware('locale:en')` en miroir de chaque route FR), locale partagée via Inertia, `laravel-react-i18n` côté front (`t()`/`tChoice()`, clés = phrase française, `lang/en.json`), `spatie/laravel-translatable` côté back (colonnes JSON `{"fr": ..., "en": ...}`, migration de backfill sans perte pour chaque colonne convertie). Bug de régression corrigé : la langue restait figée en changeant de page côté client — fixé via `I18nLocaleSync` dans `app.tsx` (générique, couvre toutes les pages). **Traduit :** catalogue, fiche produit, panier, tunnel d'achat complet (commande/paiement/confirmation), puis extension à tout le reste du storefront public — marques, guide de choix, pages légales (structure/nav, pas le corps juridique), espace compte (dashboard, adresses, historique commandes), home — chaque route a désormais son miroir `/en/...` (`routes/storefront.php`/`routes/web.php`), tous les liens internes construits via le helper partagé `resources/js/lib/locale-path.ts` (`localizedPath`) plutôt qu'en dur, `LanguageSwitcher` reconnaît tous les chemins bilingues. **Volontairement non traduit :** corps juridique des pages légales (encore `[À COMPLÉTER]`, inutile de traduire du provisoire — voir 20.1), emails transactionnels, back-office admin (outil interne, pas de version anglaise prévue). Tests Pest dédiés par route i18n (`CatalogLocaleTest.php` et équivalents pour le tunnel d'achat), vérifiant réponse 200 + `locale` correcte côté props Inertia + contenu traduit affiché. |
| 25.2 | Multi-devise | Les deux | P3 | ⚪ | `brick/money` (déjà requis en 8.2) gère nativement le multi-devise — surtout un travail de exposition/conversion côté front à ce stade. |

### 26. Support & self-service

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 26.1 | Chat / support client | Front | P2 | 🟢 | **Crisp** choisi (vs Tawk) : UI plus soignée, gratuit jusqu'à 2 agents, option d'hébergement des données en UE. Script embarqué (`resources/js/components/storefront/crisp-chat.tsx`, injection du script `client.crisp.chat/l.js` au montage), `CRISP_WEBSITE_ID` en config (`services.crisp.website_id`, partagé aux pages via `HandleInertiaRequests`). Monté dans les deux layouts publics (`storefront-layout.tsx`, `auth-simple-layout.tsx`) — jamais dans le layout admin. Décision consciente : contrairement à GTM (27.1), **ne s'appuie pas sur le bandeau de cookies** — c'est un outil de support, pas de tracking marketing, donc chargé pour tous les visiteurs dès l'arrivée sur le site. Pas de package Composer/npm, pas de modèle back, comme prévu. |
| 26.2 | Portail self-service retours (hors échanges) | Les deux | P2 | 🟢 | Décision avec l'utilisateur : retours uniquement pour cette version (les échanges — choix d'une variante de remplacement, delta de prix — restent hors périmètre) ; éligibilité = commande `delivered` depuis moins de 14 jours (`Order::RETURN_WINDOW_DAYS`, délai légal de rétractation France) ; acceptation = remboursement Stripe automatique via `RefundOrder` (16.3), pas d'étape manuelle séparée. Nouvelles tables `return_requests` (`order_id`, `user_id`, `status`, `reason`, `admin_note`, `decided_at`) et `return_request_items` (`order_item_id`, `quantity`, `amount_cents` — snapshot du montant, pas recalculé). `Order::isEligibleForReturnRequest()` : statut `delivered`, `updated_at` dans la fenêtre de 14 jours (pas de colonne `delivered_at` dédiée, le tracking colis 11.3 n'existe pas encore — `updated_at` sert de proxy, imprécis en théorie mais stable en pratique), et pas de demande déjà `submitted`/`accepted` en cours (une commande refusée peut refaire l'objet d'une nouvelle demande). Storefront : `App\Http\Controllers\Storefront\ReturnRequestController` (`create`/`store`, `mon-compte/commandes/{order}/retour`, miroir `/en`), formulaire de sélection des lignes + quantités (articles offerts exclus) + motif. Admin : `App\Http\Controllers\Admin\ReturnRequestController` (`index`/`show` sous `orders.manage` — support/staff peuvent traiter ; `accept` sous `orders.refund` — admin uniquement, déclenche `RefundOrder` pour le montant des lignes retournées ; `refuse` sous `orders.manage`, motif optionnel). Une demande déjà décidée (`accepted`/`refused`) ne peut pas être re-décidée (422) — même discipline d'état terminal que 16.3/9.4 (audit du 2026-08-05). Notifications : `ReturnRequestStatusUpdated` (client, un email par transition : soumission reçue / acceptée / refusée avec motif) et `ReturnRequestSubmitted` (admins, à la soumission) — l'email de remboursement lui-même reste `RefundConfirmation`, déclenché par `RefundOrder`, une préoccupation distincte du statut de la demande. 20 tests Pest (`ReturnRequestTest` : éligibilité guest/autre client/pending/hors délai, soumission + notifications, article offert rejeté, quantité excessive rejetée, 2e demande bloquée tant qu'une est en cours ; `AdminReturnRequestTest` : permissions support/admin, acceptation déclenche le remboursement + les deux notifications, refus notifie, état terminal verrouillé). |
| 26.3 | Wishlist partageable (lien public) | Les deux | P2 | 🟢 | Table `wishlists` migrée telle que prévue (`user_id`/`product_id` FK `cascadeOnDelete`, contrainte unique `(user_id, product_id)` — l'idempotence de l'ajout repose sur `firstOrCreate()`, pas sur une verification applicative). "Partageable" : colonne `wishlist_share_token` (nullable, unique) ajoutée sur `users` plutôt qu'une table dédiée — générée à la demande (premier clic sur "Partager mes favoris"), jamais à la création du compte, pour ne pas polluer chaque utilisateur d'un token inutile. `App\Http\Controllers\Storefront\WishlistController` : `index` (page compte), `store`/`destroy` (idempotents, scopés `auth`), `regenerateShareLink` (révoque l'ancien lien en générant un nouveau `Str::random(40)`), `public` (lecture seule, sans auth, résout l'utilisateur par le token — 404 si invalide, jamais d'accès en écriture). `App\Support\WishlistPresenter` centralise la sérialisation (réutilisée par la page compte ET la page de partage public), même pattern que `CartPresenter`. Routes `favoris/{product:slug}` (POST/DELETE, throttle 30/min), `favoris/partages/{token}` (GET public), `mon-compte/favoris` + `mon-compte/favoris/partager` (sous le groupe `auth` existant) — miroir `/en` complet. Front : `components/storefront/wishlist-button.tsx` (coeur toggle, état optimiste local, redirige vers `/login` si non connecté plutôt que de masquer le bouton) branché sur la fiche produit (6.2, disponible même hors-stock) ; page `storefront/wishlist` (grille produits, bouton copier/régénérer le lien de partage via `useClipboard` existant) ; page `storefront/wishlist-public` (même grille, lecture seule, sans actions) ; lien "Mes favoris" ajouté dans `UserMenuContent`. Volontairement laissé hors périmètre pour cette itération : bouton coeur sur les grilles catalogue/accueil/marque (nécessiterait d'exposer un set d'IDs wishlistés par page, risque de N+1 à auditer séparément) — seule la fiche produit permet d'ajouter/retirer pour l'instant, la page `/mon-compte/favoris` reste le point d'entrée principal pour gérer la liste. 11 tests Pest (invité redirigé, ajout, idempotence, retrait, isolation entre utilisateurs, exclusion des produits non publiés, génération/régénération du lien de partage, page publique accessible sans auth, token invalide → 404), Larastan clean. |
| 26.4 | Réachat rapide / abonnement récurrent | Les deux | P3 | ⚪ | `laravel/cashier` si abonnement facturé récurrent via Stripe — uniquement si des produits consommables s'y prêtent. |
| 26.5 | Formulaire de contact | Les deux | P1 | 🟢 | Page publique `storefront/contact` (nom/email/sujet/message, react-hook-form + zod) sur `GET/POST /contact` (miroir `/en`), throttle dédié (`storefront-contact`) + honeypot (13.4, même pattern que la newsletter). Contrairement à l'intention initiale, le message **est persisté** (table `contact_messages`, décidé après coup pour permettre un vrai suivi admin) en plus d'être notifié par email à tous les admins (`ContactMessageReceived`, `Notification::send` sur `UserRepositoryInterface::admins()`) avec Reply-To sur le client. Espace admin `GET /admin/messages` (liste, accès admin/staff/support sans permission dédiée) et `GET /admin/messages/{id}` (détail, marque `read_at` à la consultation) : `Admin\ContactMessageController`. **Réponse depuis l'admin** (`POST /admin/messages/{id}/reply`, throttle `admin-contact-reply`) : envoie un vrai email au client (`ContactMessageReply`, `Notification::route('mail', ...)` car pas forcément un `User`) avec Reply-To sur l'adresse de l'admin qui répond (pas `noreply@`, sinon une réponse du client atterrirait dans le vide) ; chaque réponse envoyée est aussi journalisée (`ContactMessageReplyLog`, ledger immuable façon `Refund`/`InventoryMovement`) et affichée en fil sur la page de détail. Décision consciente : la réponse du client à l'email reste **email uniquement**, pas réingérée dans l'app (pas de webhook de réception configuré — un vrai fil bidirectionnel demanderait un système d'ingestion d'email entrant, hors scope). Bug corrigé en testant en conditions réelles : `MailMessage` sans `->salutation()` explicite se terminait par "Regards," (anglais) même en contenu français — la closing par défaut du framework (`@lang('Regards,')`) n'est traduite que si l'app tourne réellement en locale `fr` au moment de l'envoi, ce qui n'est pas le cas des jobs en queue (`config('app.locale')` vaut `en` par défaut, aucune notification du projet n'appelle `->locale('fr')`) — corrigé ponctuellement ici (`->salutation('Cordialement,')`), bug plus large à traiter dans un audit (voir aussi 12.2/12.7/12.8/12.10/13.3). 11 tests Pest (`ContactTest`, `AdminContactMessageTest`), Larastan clean. **Mise à jour 2026-08-04** (ajout Turnstile, 13.4) : bug corrigé sur le formulaire — `router.post()` a `preserveState: true` par défaut (le composant ne remonte pas après le submit), et le toast de succès affiché (déclenché par un event `flash` global, indépendant de `onSuccess`) donnait une fausse confiance que `reset()` du formulaire s'exécutait bien. Corrigé en passant `preserveState` en fonction (`(page) => Object.keys(page.props.errors ?? {}).length > 0`) : le composant remonte uniquement en l'absence d'erreur de validation, garantissant des champs vides via les `defaultValues` sans dépendre du timing de `reset()`. |

### 27. Marketing & permissions avancées

| # | Tâche | Dev | Priorité | Statut | Détails |
| --- | --- | --- | --- | --- | --- |
| 27.1 | Pixels marketing (Meta, TikTok, Google Ads) | Front | P1 | 🟡 | Scripts tiers chargés conditionnellement, uniquement après consentement du bandeau cookies (20.6) — jamais avant, conformité RGPD. **Google Tag Manager installé** (`GoogleTagManager`, monté dans `StorefrontLayoutTemplate` et `AuthSimpleLayout`, `GTM_ID` en config, gate sur `hasMarketingConsent()`) + **GA4 branché** (balise "Balise Google" dans GTM, `G-XMS6F6Y39T`). L'événement `purchase` (dataLayer, montant + devise + n° commande, poussé une seule fois via garde `sessionStorage` sur `checkout-confirmation.tsx`) est déjà prêt côté site. **🔴 Suivi de conversion Google Ads bloqué** : la création de compte force la création d'une vraie campagne payante (aucun mode expert accessible, aucun lien pour créer une campagne "sans objectif" trouvé) avant de donner accès aux Outils/Conversions — reporté à quand une campagne Ads sera réellement lancée (l'ID de conversion sortira alors comme sous-produit naturel de cette étape). Meta/TikTok non commencés. |
| 27.2 | Permissions admin fines par rôle | Backend | P2 | 🟢 | Rôles/permissions `spatie/laravel-permission` déjà seedés (2.2) — le vrai trou trouvé en auditant : le frontend vérifiait `auth.roles.includes('admin')` en dur sur plusieurs boutons (refund, accepter un retour, nav catalogue) au lieu de la permission réelle, donc silencieusement désynchronisable du backend dès qu'un rôle changerait. Corrigé en exposant `auth.permissions` (toutes les permissions du user) via `HandleInertiaRequests`, et en faisant vérifier les vraies permissions côté React (`admin/orders/show.tsx`, `admin/return-requests/show.tsx`, `admin-sidebar.tsx`) au lieu du nom du rôle. `admin.health`/`admin.activity-log` passés de `role:admin` en dur à `permission:settings.manage` (la permission existait déjà mais n'était vérifiée nulle part), cohérent avec le reste du back-office. **Nouveau** : `GET /admin/users` + `PATCH /admin/users/{id}/role` (`Admin\UserController`, réservé au rôle admin strict, throttle `admin-user-role`) — première UI pour changer le rôle d'un utilisateur (avant : uniquement via `tinker`/seeder). Garde-fou : un admin ne peut pas retirer son propre rôle admin (évite un verrouillage accidentel). `reviews.moderate`/`content.manage` restent seedés mais non appliqués — réservés à des features pas encore construites (modération avis, CMS), pas de fausse UI ajoutée dessus. 5 tests Pest (`AdminUserRoleTest`), Larastan clean. |

---

## Estimation d'effort par famille de features (repère rapide)

### Facile (peu d'effort dev)

Prix barré (6.4), carrousel images (5.2), onglets fiche produit (6.2), champ liste INCI (4.3),
pages légales statiques (20.1-20.6), sélecteur de quantité (6.3).

### Effort modéré

Blog + liaison article↔produit (18.1-18.2, CRUD + relation many-to-many), Apple Pay/Google Pay
(9.3, config Stripe Payment Element, pas de package tiers), guide de choix produit (6.5, logique
conditionnelle front + UX), avis clients Trustpilot + Klaviyo Reviews (14-15, intégration de deux
SaaS — widgets + tracking d'événements, pas de module de modération à construire).

### Plus complexe (vrai module à concevoir)

- **Cadeaux à paliers** (10.2) : calcul du total panier en temps réel, ajout/retrait automatique d'un
  produit "gratuit" selon seuils franchis, gestion du cas où le client repasse sous le seuil.
  Isolé dans `app/Actions/Cart/SyncThresholdGifts.php`.

Rien n'est hors de portée en solo sur cette stack — mais ces deux derniers points sont ceux qui
demanderont le plus de temps réel de développement à cause des cas limites à couvrir proprement.
